# S11 acceptance — PARTIAL

Runtime/source identity: `dc6b4efd85af00c1ce6b416d0fac4df88a8a4d76`.
Baseline: `e64992d19a555349463246a000430247244f8208`.
Branch `agent/3d-s01-renderer-foundation`, draft PR #63; main is not merged.

| Check | Result | Evidence |
|---|---|---|
| Formal regional relief, independent water primitives/material, shoreline bands and shape-derived flow | PASS | FilamentMapView → SceneMesh → TerrainSurface/WaterVisualField; real compiled water.filamat |
| Existing terrain geometry, actual fan picking, LOD/shared positions, site constraints | PASS | TerrainSurfaceTest 902,230 checks, 400 national ray picks, max height 2.3207247; 173/177 chunks reused after mountain edit |
| Material weights, shared UV/tangents, fine/far interpolation and terrain edit cache | PASS | TerrainMaterialFieldTest 4,978,894 assertions, 174/177 chunks retained |
| National water coverage/components, exact water level, shape flow/shared edges, projected custom maps, single-cell channel/island, cross-chunk edits/undo, painted height priority | PASS | WaterLandformTest 803,860 checks; all 35 ports keep water contacts |
| Core save neutrality and legal port water/land movement | PASS | FieldFlowTest: actual legal port move, correct asset transitions and full SaveCodec equality; existing field asset 5,957,594 checks |
| Editor/custom map integration | PASS | InteractionEditorTest 32 + MapEditorContinuationTest 136 assertions |
| Materials and exact pinned compiler | PASS | tools/3d/build_ground_material.sh using matc1.56.0/version56; both hashes/consumer paths checked |
| Android APK / instrumentation compilation | PASS | clean build using JDK21, Gradle8.11.1, SDK35, Filament1.56.0; source identity embedded |
| Android lint | PASS | 0 errors, 78 warnings; includes existing presentation checks; source subsequently changes only native test locator and BuildConfig identity |
| APK ZIP CRC, existing development signature, ELF/ZIP16KiB static alignment | PASS | audit-3d-apk.py --require-16k; apksigner v2; actual16KiB-device installation NOT_RUN |
| Native S11 SwANGLE captures | PASS | run35729294588;681 assertions,54 actual Surface captures across three qualities and spans4/16/64, same runtime dc6b4efd85af |
| Physical phone frame time/GPU/thermal, full manual naval/editor interaction, final national art approval | NOT_RUN | no physical device attached |

## Authoritative geography

No changes under `core/`, `data/` or `app/src/main/assets/map/` from S10.
Detached national terrain SHA256: `c73820580e4806884c513fdf17e0b750edf48baa79280919453c7c0a142a9506`.
6,746 water cells, 180 six-neighbor water components, largest5,824; rendering does not change them. Existing tiny disconnected water regions are preserved, not silently repaired by visual code.

## Performance scope

Same host JVM harness, three fresh ground generations per run, both LOD buffers. Baseline generation1476.3/1196.6/1244.7ms; candidate first measured2593.7/2020.7/2068.3ms. CPU mesh payload remains75,716,856bytes across177 chunks. Shader package is additional; no extra water textures or per-cell Renderables. Mixed chunks have two primitives with disjoint index ranges, so primitive submissions increase there. New radius3.4 relief sampling, signed shore distance and radius2.4 flow explain the increased CPU load; it runs in the existing cancellable loading/edit worker, not every frame. Retained rebuilds still reuse geometry. This exceeds the20% diagnostic threshold for **CPU generation**, is a documented performance risk, and is not phone GPU frame time or FPS certification. See the evidence harness and logs for repeated measurements.

## APK

`0.78.0-3d-s11-rc1`, versionCode78; universal arm64-v8a/armeabi-v7a/x86/x86_64; existing development package/signature.
35,252,510bytes, SHA256 `9ec1e76393fd83f4ee3c4b2967fd609ddb9b2f80677aafdfdfd614bf60461bb1`.
The initial incremental APK had an incomplete ZIP central directory despite a successful Gradle exit. It was rejected, then a clean build passed ZIP CRC, signature, asset byte identity and static alignment. That invalid intermediate is not the delivered APK.

## Remaining limitations

- Shore silhouette displacement is **zero**: exact projected tile union protects every narrow channel, island and port. Wet/shallow material blending is present; bounded geometric shoreline rounding is not implemented. Thus S11 is a candidate, not full visual acceptance.
- River/lake/sea share a coherent shore-distance profile with shape-derived visual current; there is no separate hand-authored hydrological classification. National source does not currently use SEA cells; the native locator selects actual broad water shore topology instead.
- Only the existing directional light/dielectric specular lights water. Environment/IBL unification is S12; no SSR/refraction/transparent depth or wave-displaced mesh is claimed.
- S10 legacy SwiftShader blank-output compatibility and prior unrelated full-core failures are historical debts, not claimed fixed here. Targeted core/save/port regressions passed; the complete core suite was not rerun or represented as all green.
- First custom projection fixture used invalid source dimensions for the column-staggered case; corrected to a valid48-column source without relaxing the height assertion. The full corrected S11 suite passes.

## Additional repository workflows on the same candidate

- Installed experience comparison run35727797718: **FAIL**, cannot scroll to the existing `收起` UI control (GameSmokeRunner.experienceFlow). The workflow stopped before its pinned-before comparison, so this run does not prove baseline parity.
- Displacement/v0.28 upgrade run35727797512: **FAIL** on old-map SaveCodec import, with the explicit existing message that the old map save cannot be continued and is not removed or overwritten. The pinned v0.28 displacement exercise itself passed; its upgrade into this candidate did not. SaveCodec and the UI runner are unchanged by this stage; this is recorded rather than weakening either assertion.

Baseline evidence for the experience failure: S10 runtime1be4ddc2642c, run35725722794/job106738767837, reports the identical `UI content not reachable by scrolling: 收起` failure at12:14UTC. This confirms that failure predates S11; it does not turn the candidate workflow green. The v0.28 upgrade failure was not re-executed on the immediate S10 baseline during this turn.

Existing S10 material probe on the S11 candidate (run35727797522): **FAIL** on SwANGLE at `read actual rendered Surface` (PixelCopy). Logcat records a500ms buffer-fence readback timeout; its failure-time UI screenshot displays actual textured terrain, but no successful standalone Surface capture was produced. Ground generation took48012ms on this CPU-backed emulator with substantial GC; this is a material startup-cost concern, not phone GPU FPS. The separate S11 native probe remains the authoritative targeted run. Legacy SwiftShader also failed; neither is hidden as a pass.

## Follow-up optimization

Runtime `dc6b4efd85af00c1ce6b416d0fac4df88a8a4d76` removes transient Hex allocations from water-neighbor scans and avoids material neighbor objects outside the compact support. It preserves all field equations and mesh layouts; the full foundation/material/S11 regression suite passes unchanged. Same host generation2291.9/1790.3/1773.0ms versus the pre-optimization2593.7/2020.7/2068.3ms; payload stays75,716,856bytes. This is still slower than S10 and remains a mobile startup risk. Static material screenshots now pause presentation submissions and drain the existing queue before PixelCopy, keeping the original readback success and color-variation assertions. New native run35729294588 supersedes the interrupted earlier probe and PASSED681 assertions with54 actual Surface captures.


## Final native and visual inspection

PASS: installed x86_64 Android29 emulator, ANGLE2.1.17841 / SwiftShader5.0 Vulkan, GLES3.1; actual `SOURCE_SHA` matches dc6b4efd85af00c1ce6b416d0fac4df88a8a4d76. LOW/MEDIUM/HIGH and spans4/16/64 all completed. Camera center picking, real material presence, LOD transitions, pause clock and full-save neutrality assertions passed. Coordinates, camera and buffer sizes are recorded in s11-shots.csv; real Surface images and UI selections are included, not concept renders.

Inspected all three contact sheets (54 views). Independent broad water is visible with continuous shallow/deep tones and wave normals; port contacts and small islands remain. The mountain/gate scene has connected constrained relief. All three qualities retain land/water materials; no blank frame was observed in these captures. Art acceptance remains PARTIAL: stair-step shore silhouettes, repeated terrain detail and fragmented northern-edge contours are still visible. No geometric-rounding, final national-realism or uniform environment-lighting approval is claimed.

Native ground CPU load in later quality cycles was20318/22657ms (previous unoptimized separate probe48012ms; not a controlled device benchmark). Capture required the static-frame queue drain. Logs still contain emulator buffer-dequeue warnings, despite successful captures. The post-instrumentation meminfo probe reports no process, so PSS is NOT_RUN; callback/submission timings are not presented as GPU FPS. Physical phones/thermal and complete manual naval/editor flows remain NOT_RUN.

Latest S10 compatibility/material workflow35729294633 on dc6b4efd85af: SwANGLE **PASS** after the capture/allocation follow-up; legacy SwiftShader **FAIL** remains. This supersedes only the earlier SwANGLE PixelCopy failure, not the legacy-driver or unrelated UI/upgrade failures.
