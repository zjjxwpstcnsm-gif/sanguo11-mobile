# S13 nationwide / grid candidate

Status: PARTIAL. Do not merge PR #63. User-requested branch `agent/3d-s01-renderer-foundation`.
Baseline `238fc35b0f583ae81b0c6e1aad6692a2c4a412c8`; main `dafda0e3c3f240205a8b336dd1f6398e33cdeea6`.
Game runtime changes `8757d71aa4965e03be54464b64d95e9fdeccd5a6`; final build/test source `8cc418de45e3406865fb4b5654c8c7993ffa30aa` (canonical traditional port-name probe correction only).

## Shipped changes

- Normal game header **网格** and View-menu grid switch. Defaults off, persists in renderer preferences, applies to 2D and 3D, survives host recreation/quality changes and does not enter SaveCodec.
- Grid is independent from faction territory shading, selected cells, movement/attack/coverage/path cues and editor-specific layers. Strategic zoom hides fine grid to avoid a dense full-map overlay. Disabling grid avoids grid-only cell iteration.
- Territory fill no longer unconditionally strokes every 3D cell. This fixes visible cell borders even when the user disables grid.
- Include mapSeed in terrain chunk cache identity. This is defensive dependency completeness: present terrain fields are coordinate-deterministic, so no claim of a reproduced stale-seed visual defect.
- Added nationwide payload audit and registered production-activity native probe/workflow. No changes to core gameplay, authoritative maps, terrain rules, engine version, default 2D, GLES gates or installed assets.

## Host validation

| Check | Result | Details |
|---|---|---|
| Nationwide payload | PASS | 33,703,509 checks; 217 chunks (177 national, 40 custom); 1,146,990 vertices / 1,411,680 triangles across both LODs |
| National/custom | PASS | All valid national chunks; six valid custom maps, axial/odd-r/odd-q, origin 0/5, VOID holes, water crossing, forest/mountain/sand, seven-cell site. Full SaveCodec unchanged |
| Geometry/material | PASS | Finite position/normal-frame/material channels, bounds, legal indices, normalized weights; duplicate global edge height/UV/frame/weights, exact coarse/fine shared vertices; water-specific flow channel intentionally not compared against land macro color |
| Existing host matrix | PASS/FAIL | 13 PASS, 2 FAIL: map61 `versions independent`, turn48 `AI uses deployment commands`. Both same failures reproduced on baseline |
| Full Gradle check | FAIL / PRE_EXISTING | 31 failed core tasks, identical task set and assertion messages on S12 baseline. No disabled tests or relaxed assertions |
| S12 environment | PASS | 1,584,011 assertions; all 167 authored mesh normals/animated frames, vegetation exclusions and LOD |
| Shipped assets | PASS | Eight periodic terrain textures/materials and 175 environment manifest hashes; unchanged runtime assets |
| Build | PASS | assembleDebug, assembleDebugAndroidTest, lintDebug; 79 tasks; final source identity compiled into APK |
| Native S13 | NOT_RUN | Current-source run 35735972865 pending result; never counted as PASS merely because queued/running |
| Physical phone | NOT_RUN | No attached device. 20-minute thermal/GPU/frame-time/16KB-device installation not measured |
| Full manual editor / SAF | NOT_RUN | Host editor checks pass; not a substitute for system file-picker/user actions |

Commands: `bash scripts/test-3d-nationwide.sh`; `JSON_TEST_JAR=/tmp/s03-json.jar bash scripts/test-3d-environment.sh`; `bash scripts/test-3d-final.sh <evidence-dir>`; `gradle check --continue` on candidate and detached baseline; `gradle :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug`. Local toolchain full JDK21 with host `--release 17`, Gradle8.11.1, Android35 and Filament1.56.0; init file selects available JDK21 for Gradle's Java toolchain without changing source/target17. Initial environment attempt lacked `javac` on PATH; rerun with full JDK PATH. Initial new synthetic fixtures were not valid complete saves; corrected their sites, source dimensions, padding and projection rather than weakening SaveCodec checks.

## Resources and APK

Same-host national terrain CPU arrays: **75,716,856 bytes**, identical baseline/candidate;177 chunks. Three cold/cache samples are in evidence; concurrent build load makes elapsed-time samples unsuitable for a speedup claim. These are CPU arrays, not measured GPU residency or FPS. Grid adds no models/textures; existing Canvas overlay draws only detailed visible cells. Existing S12 forest memory increase is not resolved by this change.

Universal APK version `0.80.0-3d-s13-rc1`, code80, package `game.sanguo.mobile.dev`; four ABIs. **35,355,958 bytes** (same aligned ZIP size as baseline, different contents/hash).
SHA256 `f2583a1193a47519c00e746941ca1d2aae76fde7d22ac00a03f0eb48e73ae64d`.
Original development certificate SHA256 `8f64ee37f8ff58de8f5a199aac2ae745a5bc927d0d0eabac7540083a5e551f24`.
V2 signature, ZIP CRC, embedded `8cc418de45e3`/grid UI strings, static ELF/ZIP16KiB alignment PASS. No claim of phone installation.

## Remaining acceptance and implementation debt

Native screenshots, fixed-camera before/after inspection, operation recording and measured device memory remain pending current-source native run; physical-device GPU/thermal/FPS remains NOT_RUN regardless of emulator outcome. Full gesture-driven editor/import/export, all old saves, naval battle and playback manual matrix remain incomplete. The S11 shoreline still follows exact stepped tile silhouettes; rounded shore geometry is not implemented. S12 forest silhouettes and ground/site art remain stylized, and native images must decide visual quality. These are real open items, not “100% final art acceptance.” Current candidate is not proposed for final merge/release.

Native probe preflight caught simplified-name lookup against traditional canonical port names (中廬/烏林/陸口/濡須/頓丘). Test source8cc418d corrects the exact names; game names/data are untouched. Previous run is not accepted as evidence.
