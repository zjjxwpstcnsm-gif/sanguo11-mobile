# S11 landform and water handoff — PARTIAL candidate

User-selected branch: agent/3d-s01-renderer-foundation, PR #63. Do not merge main.
Baseline: e64992d19a555349463246a000430247244f8208 (S10).

The normal FilamentMapView uses one vertex/index allocation per visible chunk with disjoint land/water primitive ranges. SceneMesh.landIndexCount splits the index buffer; fine LOD preserves the partition and exact coarse surface. No duplicate blue ground underneath water, no transparent water sheet, no vertex animation, no new negative height. Water and protected site surface remain Y=0; MAX_HEIGHT remains 2.6. Existing meshHeight/rayHeight/object/overlay contracts remain valid.

TerrainSurface v2 expands compact support to 3.4 world units with broad world-anchored ridges masked by original mountain distribution. Existing road, water, VOID and site constraints cap relief. Legal explicit painted heights are exact at cell centers, blended over radius .5 and subject to existing protected-surface limits. Chunk fingerprint v2 includes an eight-cell halo, all original projection/dimensions/terrain/site/override inputs and WaterVisualField version.

WaterVisualField computes exact distance to the union of projected cell rectangles, capped at 1.5. Silhouette displacement is zero: all single-cell channels, islands, port contacts and invalid edges retain their exact footprint. Smoothing is material-only (.38-world-unit wet land band and continuous shallow water); geometric shoreline rounding remains an acceptance limitation. No global connectivity cache or per-frame map scan. Flow derives from a positive definite local water-extent tensor, support radius 2.4, with a fixed downstream reference. This is visual flow, not hydrology or navigation depth.

Vertex streams remain 7 + 8 floats. UV0 canonical (x,-z); TANGENTS as S10. UV1.x signed visual shore distance. UV1.y land macro tone OR water flow angle, selected by disjoint triangle ranges. Bank weights remain COLOR. Never use the water angle as land macro tone. flipUV=false is mandatory for both shaders. Direction interpolation stays within a single hemisphere. Procedural shader normals need no water texture upload. Exact matc1.56/version56, opaque lit water, view-dependent dielectric specular, stable world phase, no screen UVs. Low quality reduces normal amplitude; reduced motion freezes the clock. Background resets tick origin, not accumulated phase. Clock closes at4096s and is independent of core playback.

Existing controlled worker/generation token, resource owner, visibility/upload budget, LOD, thermal gate and safe2D recovery remain. Both primitives are counted in diagnostics. WaterMaterial is released after mesh resources. Every edit updates both ranges and all auxiliary data through the same bounded chunk fingerprint. S12 can add environment/IBL to the existing shared lighting and use the signed shore distance for site blending without moving ports or altering map bytes.

## Validation and build identity

Runtimedc6b4efd85af00c1ce6b416d0fac4df88a8a4d76; APK0.78.0/code78. Host S11 suite803,860 assertions; existing material/terrain/editor/port/save suites PASS. Clean APK passes ZIP CRC, existing signature and static16KiB alignment. Read acceptance/s11.md for actual CI results and gaps, including the two unrelated repository workflow failures. Next stage must not mark geometric shoreline rounding or physical-device performance complete.

Native run35729294588 PASS:681 assertions/54 Surface captures on Android29 x86_64 SwANGLE, runtime dc6b4efd85af. Contact sheets inspected at every quality/span; material water works but stair-step silhouettes and repeated terrain detail remain. PSS unavailable after instrumentation exit; physical-device performance untested. S12 must read the visual limitations rather than infer full realism from an automated pass.
