# v0.50 高精度参考图分区还原

基线为主分支完整 v0.49，`18b1c2d01d6ef5d0bc63fef8a3e483186a99e390`。不回滚战斗、忠诚、粮耗、回合播放、UI或美术。

## 参考与复核方式

用户提供 `MAP_SAN11.rar`，内含 `MAP_SAN11.jpg`（7200×6752）。图上署名 X GOD / 艾克軋德，日期2018-05-21。原图仅用于用户请求的对照，不随公开仓库或APK再分发。原始文件SHA-256、42城配准点、45港关旧/新坐标、分区多边形、道路、水面掩码和人工窄水道修正记录在 `data/map/reference-v050.json`。

以42座城市图标中心拟合图像像素与既有原始坐标，使用相同2:1移动版缩尺，不把透视截图当精确游戏数据。各港关按原图分块放大目视定位；贴岸位置最多一格适配，必须能通过实际的odd-r转轴坐标后上下船。原图白色陆路与灰色水面分别判读；绿色地区边界遮挡的洞庭东口另做单格人工校正。

## 本次进入正式地图的内容

42座城的原有坐标与配准一致，保持不动。10关、35港逐一复核，40处改变位置，5处维持原位。全国正式网格仍100×100，实际修改3,535格，九剧本及其校验目录同步。

|分区|本次处理|
|---|---|
|武威—安定—天水|西北沙地基调、陇右连续山脊、林带和城间平原通路|
|关中—洛阳—太行南口|黄河岸线、潼关/函谷/武关/虎牢、洛阳周边山系、壶关道路转折|
|汉中—剑阁—成都—江州|阳平/剑阁/葭萌/涪水/绵竹位置、盆地与分叉道路，不再把关卡互相错置|
|荆襄—江汉—洞庭|汉水/长江/洞庭连接，房陵/中庐/湖阳/夏口/汉津/乌林/江津等贴岸位置|
|濮阳—徐州—淮南|连续森林与山岭、河岸及寿春庐江邻接通路|
|江东—鄱阳沿岸|江都/曲阿/虎林/句章/鄱阳/庐陵等，江岸、山林及吴会稽通路|

壶关在原图上位于晋阳—邺道路向南绕行的拐点，不应再用“y必须介于两城之间”替代原图。曲阿处于吴的西北江岸，不沿用前版为“离吴小于等于4格”而强行靠近的点位。对应过时测试已替换为实际源图关系，另加精确坐标与通行验证。

## 可玩性保护

保留87个据点及所有ID、武将、时代归属、开局资源和任务；保持每座城市开发容量，591处全国开发位中110处因港关、山口、水面或通行道路冲突就近等量搬移，仍在原城市7格内。不通过删除容量“修复”冲突。

每个港口保留有陆地纵深的上/下船通道，在所有开发位建满后仍验证双向合法dock edge；未拥有港口时两向均不可穿越。20个汉水/长江/湖区港口属于同一连续水域，不是互不连通的装饰水塘。关卡的单格缺口可能让六角格部队从外角一步进、一步出而绕过控制，因此以贴山双格隘道约束，使用正式`Army.moveCost`检查友军可过、敌军在该山口不可绕行；不修改战斗或寻路规则。

## 复现与验证

`tools/content/refine_geography_v050.py --apply`是**仅针对v49保留的v45地图摘要**的一次性、离线、确定性迁移：先验证18项旧摘要再执行，重复执行拒绝。无需原始JPEG、网络、OCR或运行时地图覆盖层。`tools/content/test_geography_v050.py --base <baseline>`验证注记与九剧本接缝、全部非地理记录、开发容量、上下船、主水系以及旧美术原字节；`MapFidelity50Test`直接用正式世界与移动规则验证建满设施、友敌港口、10关、14条陆路和保存读取。

执行入口：`bash scripts/test-map50.sh`（包含v49近期回归），`bash scripts/test-ui-models.sh`。Android安装探针：`-e map50 true`，从正式剧本加载并截图八个区域、全图、横屏，验证视角操作不改变世界；另复跑`systems49`及`flow48`。

## 边界

这不是原版游戏地图文件的逐格导出。100格地图上的山脊/森林是人工概括，并保留部署空间、港口通路及城市容量。东北及南中未进入六区陆地精修的部分仍有前版地形；不能凭截图唯一确定的湿地/浅滩细类在保留岸地上沿用旧配置，不伪称已全部考据。北部细支流和全国所有支路未做逐一航行/通关验收；本轮已验证的主水域和关隘范围见测试。

不强制迁移已进行中的存档：旧存档嵌入其原地图，继续读写时保持原状。请新开剧本查看本轮地理；无保存格式改动。ARM真机和手机帧率须以真实设备实测为准，不以模拟器截图替代。
