你接手 sanguo11-mobile，执行“R00–R14 未完成项整改与完整复验”。

这不是新一轮仅审计任务，也不是 R15。请承接已有正式成果，按依赖顺序修复真实缺陷、补足不合格资源和缺失验收，交付新源码对应的 APK、真实运行证据及更新后的同一个 PR。不要只修改文档状态、添加检测脚本或回复下一步计划。

一、仓库、分支和任务边界

仓库：https://github.com/zjjxwpstcnsm-gif/sanguo11-mobile.git
串行分支：agent/native-pc-visual
现有 PR：https://github.com/zjjxwpstcnsm-gif/sanguo11-mobile/pull/67

已知审计检查点，仅供确认祖先关系，禁止据此回退：
- main / 架构 PR66 合并点：ac29b458325b52d6e302ca44270d16552de4ed7f
- 审计前输入：eeb09b4b0037a482b440816ec2fe718c9701ab75
- 已有生命周期修复：7625ad31492fed97486937cbb1131a5ee35e6688
- 上轮 APK source：badd7e56308b4f6a47d60353d49023be8fc492a2
- 上轮 evidence-only HEAD：47e5db471e6dadb22d4c782522f97c113601461e

先读取远端最新 main、串行分支、PR、CI、已合入架构和本地工作树。以上 SHA 不是“永远最新”，应续作实时远端成果。不得 reset --hard、force push、覆盖其他 Agent 的修改、删除分支、合并其他 PR或将本 PR 合入 main。保留 Draft。

本轮只整改 R00–R14。下面的整改批次不是新 R 阶段，不自动执行 R15–R18，不启动全国美术扩张或另一次引擎迁移。

二、必须先读取的资料

1. sanguo11-native-pc-visual-agent-prompts.zip：
   完整读取 00_GLOBAL_RULES.md、01–09 全局文档，以及 prompts/R00_PROMPT.md 至 prompts/R14_PROMPT.md。特别遵守渲染、资产、视觉、测试和 Git 交付契约。
2. sanguo11-native-r00-r14-audit-evidence.zip：
   读取 checklist/ 下完整逐条表、host/、baseline/、candidate-raw/、source-changes/、patch/、REMOTE_DELIVERY.json 与历史记录。
3. 仓库 docs/native-pc-visual/：
   PROGRESS.md、DEFECTS.md、reports/R00-R14-AUDIT.md、各阶段 reports/handoffs/evidence，以及 checklist-index。
4. 仓库 AGENTS.md（若有）、docs/architecture/、既有原生 3D 文档与实际正式调用路径。

附件暂不可用时，先从仓库和原发布制品找同份资料，不根据摘要编造原要求。确实取不到的条目写明来源缺失；不妨碍继续修复已经明确、可复现的问题。

原版完整性以原提示词为准，审计摘要不是额外技术要求。例如原契约允许适配现有流水线的刚体动画，并不强制完整骨骼、全脚 IK、体积烟雾或实时 GI。应修真实观感和同步缺陷，不因审计提到“没有某高级技术”就重写引擎。

三、不可突破的边界与完成判定

- 保留 Filament 1.56.0 / OpenGL 和匹配的离线材质编译器，不重启 Unity，不新增另一套 GLES/GLSurfaceView 帧循环，不无依据升级依赖。
- 保留 GameSession 唯一权威、game-api/game-runtime 架构和不可变显示投影。渲染器不能直接写 World，不能重算伤害、射程、通行或 AI。
- 不改全国地图地形类型、邻接、城港关坐标、七格占地、港口上下水、关隘、规则 RNG、玩法规则或 SaveCodec 语义来迁就画面。验证编辑器可在隔离测试存档内通过既有事务改地图，但不能修改正式基础数据。
- 保留已经修好的 MapHost + SceneRenderGate。确有新证据发现门控缺陷可做最小修正，但不得恢复被遮挡宿主持续提交或多个后台帧循环。
- 不清除用户存档、不卸载用户应用凑安装成功。pm clear、破坏性故障注入和进程终止只可用于明确隔离的测试环境。

将逐条清单区分为：代码缺陷、资源/视觉不合格、尚未执行的验证、外部依赖不足、历史追踪缺口。复合条目拆成可执行子项，保留原 ID 与原文。

每项记录：复现输入、根因/待证假设、修复文件与入口、新增回归、证据、当前状态。整体用 NOT_STARTED/PARTIAL/BLOCKED/PASS；子项用 PASS/FAIL/NOT_RUN/NOT_APPLICABLE，后者必须解释。

“代码已改”“功能运行通过”“美术通过”“真机通过”分别记录。某项只是没测，应先测，不能无依据重写；已测失败必须修，不能重新标成仅待验收。历史证据缺失可以用当前源码的新复验建立当前结论，但不能倒填为旧版本当时已经通过。

四、第一优先：修复全图势力预览与陈旧日期画面

A. 全图势力预览超时（R12，关联 R00/R01/R03/R06）

已知上轮完整 NativeR12Instrumentation 在真实 ScenarioFactionPicker 预览失败：pending=614、output=WAITING_FRAME，后续确认势力、开局和命令流程未到达。这些是旧复现数值，不应硬编码成新断言。

先在同配置下复现，沿 MainActivity → 预览宿主/MapHost → FilamentMapView → CPU 队列 → GPU 上传 → 实际帧输出定位。至少区分：
- 窗口焦点、可见性、Surface/viewport/相机尺寸是否正确；
- 请求是否每帧换 generation、重复入队或反复失效，导致结果无法提交；
- 不可见块/装饰预热是否挤占首屏，是否一次生成全图高精度内容；
- pending 是 CPU、待上传、过时任务、缓存预热还是可见内容，是否存在不能收敛的计数；
- 上传预算、beginFrame/endFrame 和首个真实呈现是否受阻。

根据证据做最小必要修复：复用现有分块/队列，正确使用低细节总览、视锥优先级、去重、取消、背压和分帧上传。完整地图和势力必须保留，不能裁成小地图、减少对象、跳过预览、换固定图片或暗中回退 2D。

首屏加载可以渐进细化，但不能借“已有一帧”跳过必需地图、交互或队列稳定性验收。若重新区分可见任务和后台预热计数，必须说明语义，补足两者的真实完成/有界性检查。禁止把 READY 写成常量、仅放宽超时或删掉原测试的失败条件。

修复后完整重跑原 R12 安装测试，并补真实输入链：
正常冷启动 → 新剧本 → 全图势力预览 → 切换/选择势力 → 确认开局 → 选城/出征 → 选军/移动/攻击 → 战报 → 下一旬 → 保存/读档。

测试夹具只能准备可复现场景，不能用直接调用业务方法代替正在验收的 UI 按钮/手势。不得只从 auto.sg11 绕过前半段；不得在第一次断言失败后宣称后面的流程已通过。

B. 日期和播放覆盖层不刷新（R12/R14）

已知七月、四月 fixture 的 authority/snapshot/TextView 内容正确，但真实合成截图仍显示一月；历史还有跨月后显示旧月份和播放进度40%的画面。日志出现过 HWUI dequeueBuffer -110，但根因尚未证实，不得直接定性为模拟器问题。

定位完整链：权威提交/revision → UI订阅和主线程更新 → 实际可见控件/ViewRoot → layout/draw → Surface与窗口合成 → 截图/录屏。排查旧 Activity/视图引用、失效监听、仅更新隐藏控件、主线程被同步解码/等待阻塞、错误失效传播、Surface层级和采集到旧缓冲区。

用同一检查点的权威日期、控件内容、UI绘制/帧标识、3D Surface捕获、整屏原图和连续录屏交叉确认。不能仅凭 getText()、可访问性文本或 PixelCopy 的局部结果宣布屏幕正确。

若证据表明只有采集链失真，先在未修改 APK 上证明并修采集工具；若是实际显示问题，就修改正式 UI/调度/合成路径。禁止为验收加入硬编码月份、每帧重建页面、强制重启 Activity、测试专用重绘或未经诊断的全局软件渲染开关。

验收必须包括：新开局、不同月份读档、真实三月下旬→四月上旬、跨年、播放结束/跳过、关闭对话框返回、前后台和 Activity 重建。每次稳定后可见日期/旬、进度与权威/播放状态一致；UI文字不受世界后处理污染。

五、第二优先：补齐异步资源与生命周期（R00/R01/R06）

检查 FilamentMapView 初始化中的 decodeStream、纹理图集、GLB/rig 描述等同步 CPU 工作。复用现有有界队列，把耗时读取/解码/转换移到后台；Filament对象创建、GPU上传和销毁仍在经验证的 owner thread，不能仅把整个构造函数扔到线程池。

后台结果带会话、地图、视觉、资产和请求版本；换世界、退出、Surface重建后过期结果不能回灌。像素和几何缓冲区必须遵守真实上传完成时点，不能提前回收；活跃共享资源不能被预算驱逐。补 live/cache/pending 与耗时诊断，避免无限预热和重复解码。

验证：20次2D/3D切换、20次前后台、快速读档/换剧本、延迟worker、Surface/Activity重建、构造中失败、缺失/截断/越界资源、低内存回调和退出重进。资源计数应进入稳定范围，不能靠每次销毁全引擎掩盖泄漏。

保留可诊断的2D安全回退与异常启动保护。Java try/catch 不得宣称能捕获 native abort；进程死亡与原生崩溃保护分别测试，故障注入不得进入普通用户启动路径。

六、第三优先：真正修复 V1/V2，而不是继续铺占位资源（R03–R09/R14）

复用正式洛阳样板及已有代表区；不得另造专用漂亮演示地图。首先关闭 V1，再使同一真实区域内地形、水岸、城港关、植被、道路和光照共同达到 V2。

A. 地形与材质（R03/R04）
当前8/24/72三角形细分共享 canonical fan 平面，不可再以三角形变多证明山体更精细。审查连续高度场的实际自由度、区域山脊/谷地、边界量化和约束，修正导致格状山包或低平轮廓的部分。

各LOD从同源全局表面/约束派生，细节须实际影响合理轮廓；跨块高度、法线、材质和拾取统一。处理LOD接缝与锚点误差，保留城门、关口、水陆格中心及通路语义。不得用裙边或大雾遮住错误。

地表采用协调的宏观区域和细节尺度，检查色彩编码、采样、混合权重及重复纹理。不能一格一色，也不能用全图模糊渐变假装连续；修复后单格编辑仍只失效必要邻域。

B. 水体与岸线（R05）
从现有权威 water mask/港口邻接派生受约束的视觉轮廓和浅岸过渡。改善硬台阶，但不得曲线穿过陆格中心、填断窄河、移港口、改变通行或掩盖禁航水语义。

验证弯道、合流、湖岸、窄口、孤立水块、跨块/边界和水陆编辑；检查自交、接缝、z-fight。用真实部队上下船和沿河行军确认船不穿岸，码头/路径/拾取采用一致水位。

C. 建筑、设施与地基（R07）
真正提升仍标 transitional 的轮廓和材质：城墙、城门、角楼、主楼、内城建筑密度及地基。按原要求检查至少三类区域风格；共享模块可以，方盒换旗色不算地域差异。

港口朝向来自真实岸线和上下船口，关卡衔接真实山路；设施覆盖全部实际类型、等级及现有状态。修正浮空、穿地、码头落旱地、门洞封路等问题。遵守七格占地，不使用模型碰撞改通行。自定义移动/新增/易主/受损/删除后显示和资源释放正确。

D. 植被、栈道和农田（R08）
提升树族、灌木和山石资产，采用稳定簇状分布、林缘/空地/区域密度及LOD滞回，排除道路、城门、港口、单位和水域。修正孤立木板、断开的栈道和浮板农田；遵守仓库已有 ROAD/PLAIN 等视觉约定，不把全部道路画成木板。

不能仅靠增加树数遮挡地形；叶片透明、mip、合批/实例化和过绘必须有预算和实测边界，低档不能退回纯色拼图。

E. 正式资产与局部验收（R06/R09/R14）
修改必须产出真实运行时模型/贴图/材质及可复查源文件或生成脚本，接入正式映射。记录来源、许可、版本、hash、LOD、bounds、pivot和支持子集；不能只交图片、材质参数或改“最终版”文件名。

每个受影响代表区至少3缩放档×2方向，记录画质、季节、网格/势力开关，并附跨块移动/旋转录屏。对照可用PC参考，逐项写图号与差异；缺参考标 REFERENCE_MISSING，不捏造版本、镜头或季节，不拿1×1占位图验收。

四季统一地形/树叶/农田/水体与光照响应，不能只套褐色滤镜；UI保持独立清晰。检查阴影漂浮/条纹/闪烁、曝光、雾排序和季节切换资源稳定性。

V2未通过，不可批量复制未达标模板并宣称全国美术完成。共享渲染/资产修复可自然影响其他区域，但本轮不执行R15的全国扩展验收。参考或设备不足时继续可做的源码/资源工作，相关门槛保留未通过。

七、补齐其他功能与动态验收

1. R02/R12 坐标和交互：
检查内部渲染尺寸、窗口尺寸、安全区、受支持方向和面板遮挡；实际测试缩放/旋转/不同分辨率触点。网格×势力四组合独立持久化；移动、战法位移、设施、围城范围直接对应权威集合。不能仅看按钮存在，必须走真实回调并确认没有漏触或重复命令。

2. R10 部队：
按当前真实兵种/装备/船型/运输类别重新核对资源和动作覆盖。修复兵种不可辨、马匹/器械不合理运动、脚滑/漂浮、船穿岸和LOD相位重置。模型、部件动作和展示位置受权威路径/事件约束，不靠root motion改规则。
录制步兵、骑兵、器械/运输、水军的真实移动与相应攻击动作，检查城门/山路/弯道/港口；缺类别的夹具须标明。刚体/骨骼和CPU/GPU实现如实记录，不为高级技术名词重写可用流水线。

3. R11 战斗：
检查实际事件枚举及映射，补全 CALM/EXTINGUISH 等真实已有类别，测试中未产生的事件不得算覆盖。效果和数字来自正式结果，播放/跳过/倍速/暂停/出视野/重建不得重发伤害或重复事件。
修复火场残留、播放卡住、错误位置、不可读烟尘和无界资源；采用适合移动端的效果，不强制体积渲染。交付全过程录像、事件次数和最终状态对等证据。

4. R13 编辑器、头像与导入导出：
通过真实UI完成地形笔刷、城港关新增/移动/修改/删除、既有撤销重做、保存/关闭重开、作为新剧本游玩、patch导入导出；验证2D→3D和3D→2D一致。
地形、水岸、植被、建筑基础和朝向应正确局部失效，旧worker不覆盖新编辑，未影响块身份保留。
补旧档、自定义武将属性/关系和头像导入的完整闭环；超限、损坏、未知版本、路径穿越和失败导入不得污染当前会话。
明确区分内部原子保存与外部SAF DocumentProvider能力。可采用本地staging、校验、优先新文件导出和读回确认；不要对不支持原子替换的provider无条件覆盖旧有效文件。验证取消、撤销URI权限、断写、进程终止和冷重启，失败必须保留原存档及可用恢复点，不能先报成功后静默丢数据。

八、回归、性能与缺失设备的处理

为每个真实缺陷建立修复前失败、修复后通过的回归；纯缺测项不必伪造失败。不要只做单元测试：必须分别报告主机、Android构建、安装运行、屏幕/动态视觉和物理设备结果。

相同存档、命令、规则RNG，在2D/3D/正常播放/关闭动画/跳过下比较完整权威状态，覆盖七格出入/攻击/进驻、港口、关卡、行军、下一旬、存读档及编辑器相关功能。选择/相机等纯表现状态按原契约单独比较。

完整core的 AI uses deployment commands / CoreTest.logistics:75 是已知继承失败，必须在整改输入和候选版独立复现、保留原退出码。若无本轮引入的规则回归，不得为了全绿改变AI、删断言或改预期；将其作为独立规则债务，不把“分类任务成功”称为完整core通过。新增或本轮受影响的失败必须修复。

性能按06_TEST_AND_PERF原口径记录：相同场景/画质/分辨率，区分CPU生成、上传、真实呈现、GPU估计和进程PSS。抓取前核对存活PID/前台场景；No process found不算零内存，回调间隔不算呈现FPS。

可用环境内跑真实压力：全国总览、密集城林、水港、实际大量部队、同时战斗、快速跨区域、编辑和换季。参数/样本数必须公开，不能删AI或改地图减负。遵守原预算，不擅自以软件模拟器计时替代手机性能。

有授权可用ARM64设备就实测全流程与30分钟混合操作，并记录型号/API/驱动/温控；Adreno/Mali分别记覆盖。没有真机或相关驱动时写NOT_RUN、具体缺失条件与可执行测试入口；继续完成不依赖这些设备的工作，不因缺真机停止全部整改，也不能宣称设备验收通过。

九、CI、证据与交付

保留原失败证据和历史报告。旧R13“资产完全不变”检查与后续R14合法材质变更冲突，应改成适用当前整改的保护：规则/基础地图树严格保护，允许的视觉变更逐文件登记manifest、版本、来源及回归。不能忽略全部assets变化，也不能删除受影响测试。

上轮“298资产与旧包一致”不是本轮禁止美术修改的要求。本轮应校验新源码的全部正式资源与新APK逐文件一致，不能继续交旧资源包。

修复测试必须真正进入CI；保留各子命令退出码，最后的验证状态应反映必需检查失败。允许失败时仍上传候选APK和诊断，但不得把上传成功或collect步骤成功当验收通过。

更新PROGRESS、DEFECTS、受影响R00–R14报告/交接与独立manifest，增加整改总报告和逐条追踪表。R03/R06/R07历史状态不一致和R10/R11清单缺失，应补当前证据索引，保留历史原文，不倒填旧PASS。

每个整改批次完成“正式修改→受影响验证→聚焦提交”，同一串行分支串行推进。最终以明确提交SHA构建和运行，不读取易变latest；版本号在远端当前版本上递增，保留applicationId和既有开发签名。

核实真实Gradle任务后执行包含以下目标的构建：
./gradlew --no-daemon :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug -PnativePcVisual=true -PunityBridgeProbe=false -PmapEditorProbe=true

验证脚本应使用仓库实际入口；至少完整重跑原R12、审计生命周期探针、受影响R00–R14脚本及架构/数据边界检查。

交付必须包括：
- 独立可下载安装的ARM64或universal APK，明确不是旧包；
- 完整source SHA、APK SHA256、包大小、应用ID、版本、ABI、证书指纹、实际后端和构建命令/CI；
- 原始截图/连续录像、目视审查时间段和结论、日志/退出码、测试夹具及逐条整改表；未观看的录像必须明示；
- 真实commit/push，并更新同一个PR67；末尾复读远端HEAD、PR与制品确认；
- source之后若仅追加证据，分别记录source SHA/evidence-only HEAD并证明仅文档差分；任何生产修改都必须重建。

GitHub瞬时写入错误先读实际结果再有限重试，不重复创建PR/提交。权限或构建环境确实阻塞时交付已完成源码/补丁及原始错误，不伪造push、APK或链接。

十、执行与最终回复要求

现在从第一优先的两个运行故障开始，依据实测根因修改正式代码，再按上述依赖顺序覆盖其余未闭合项。源码修改、验证和交付是主任务，文档随结果更新，不要再花整轮只整理清单。

不要笼统回复“全部完成”或“只剩真机”。最终必须列出：本轮真正关闭的缺陷及证据；每个仍FAIL/NOT_RUN项及原因；V1/V2结论；实际设备与测试结果；APK/证据/PR；完整源码SHA和复读远端HEAD。

若本轮容量不足以全部闭合，优先交付已经形成完整修复与验证闭环的高优先级成果，提交可继续运行的版本，逐项写清剩余代码缺陷、外部阻塞与下一执行入口；不要伪装完成、留下未经验证的半套重构或承诺会话外后台工作。

不合并main，不开始R15或其他新阶段。
