# R01 handoff

Continue agent/native-pc-visual and PR #67. Do not merge main or restart Unity.
Backend: Filament 1.56.0, OpenGL, Android main Looper owner.
Read reports/R01.md, DEFECTS.md and OWNERSHIP.md before any later work.

Production entry remains MainActivity -> MapHost -> FilamentMapView. MapHost
submits the GameSession identity before immutable scene projection. SceneWorkQueue
replaces the old unlimited executor/post-result combination. World replacement
invalidates work even if identical ground would previously have been reused.

Run bash scripts/test-native-r01.sh, test-architecture.sh, test-3d-foundation.sh,
test-3d-interaction.sh. Installed stress runner is NativeR01Instrumentation;
.github/workflows/native-r01.yml checks out exact PR head and builds the universal
native APK plus instrumentation and lint before the 20+20-cycle emulator test.

Keep R01 limitations explicit: full async asset/pose decoding, native upload/abort
fault matrix, standalone editor session/event matrix and physical-device long-run
measurement are not completed. No reference screenshots, no PC similarity pass.
Whole-map terrain cache and synchronous texture initialization are retained.
R02 is not started. Any future GPU task must retain the actual visible-map gate;
resolve newly failing current-source lifecycle checks first. Do not accept a
successful APK build as lifecycle or art proof.
