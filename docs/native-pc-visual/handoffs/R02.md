> 2026-09-25 续作复验：本阶段整体仍 **PARTIAL**。当前 APK source `ef54951dbb7056beaa782f97a3486eefa3a88ef9` / v105；相机、上传预算、输入对照、实际窗口与未闭合门槛见 [本次续作报告](../reports/R00-R14-CONTINUATION.md) 和 [213条原文清单](../evidence/R00-R14-remediation-checklist.json)。下文是历史阶段记录，不倒填为新版已经通过。

# R02 handoff

Continue agent/native-pc-visual and draft PR67, not main or Unity. This turn only R02.
Read reports/R02.md, R02_COORDINATES.md, DEFECTS.md and evidence/R02.json for final
source/build/runtime identity and remaining gaps. Never infer graphical acceptance
from host-only checks.

SceneCamera now projects both X and Z for all production callers. Legacy one-axis
helpers remain solely for old zero-yaw test probes. Future production code must use
screenX(x,z), screenY(x,z,y), worldX/ worldZ(sx,sy,y). Camera basis, ray intersection,
labels, overlay, culling and lookAt must change together. GridLayout and gameplay
adjacency were not modified. TerrainSurface ray intersects current eight-triangle
fan geometry and uses MAX_HEIGHT as its search bound. If R03 changes geometry or
height limits, update picking from that same source and rerun independent triangle
oracle tests, including steep custom height patches and true occlusions.

Commands still enter the sole GameSession. Entity/label selection maps current
visual position to stable entity's authoritative hex. Command targeting bypasses
label/model assistance; do not introduce automatic attack/enter on model click.
Long press is camera-only. Match input to view pixels, not render-buffer pixels.

Run scripts/test-native-r02.sh, test-native-r01.sh, test-architecture.sh,
test-3d-foundation.sh, test-3d-interaction.sh. Installed runner NativeR02Instrumentation
uses actual MainActivity and official scenario with Surface capture/gestures,
resolution presets, mode restore and existing command/turn/save parity. Retain R00
startup and R01 lifecycle gates. APK alone cannot prove either.

Remaining physical/visual/manual gaps prevent PASS. R03 is NOT_STARTED; do not
silently mark R01 fault/decode or prior art debts resolved.
