# R03 handoff

Continue agent/native-pc-visual / PR67 only; no automatic merge or next stage.
Read reports/R03.md and DEFECTS.md. Production is FilamentMapView camera-window
requests → SceneMesh.ground(..., TerrainWindow) → TerrainSurface v3 shared field.
The two-argument ground overload remains for whole-map reference tests, not the
runtime streaming path. All LODs preserve canonical fan geometry and picking.

Run scripts/test-native-r03.sh, test-native-r02.sh, test-native-r01.sh,
test-architecture.sh, test-3d-foundation.sh and test-3d-interaction.sh.
NativeR03Instrumentation enters coalition-190 through MainActivity and captures
home matching R02, a deterministic real chunk-seam ridge (3 spans × 2 yaws),
grid on/off, four actual drags, and existing independent commandFlow.
Workflow native-r03.yml checks out exact PR head, builds universal APK/tests/lint,
then runs API29 x86_64 SwANGLE. Do not interpret that as ARM64 verification.

Remaining work: PC reference/physical phone, shoreline hard constraints,
whole-map vegetation memory and R01 decode/native-failure debts. Do not claim
three planar precision levels improve mountain silhouette by subdivision.
