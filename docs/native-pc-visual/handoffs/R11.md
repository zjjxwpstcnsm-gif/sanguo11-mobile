# R11 handoff — PARTIAL; do not merge or start R12 automatically

Continue serial agent/native-pc-visual / Draft PR67. Preserve main ac29b458,
architecture PR66, R00–R10 assets, gameplay formulas/maps/RNG/save format.
Read reports/R11.md, R11_EVENT_COVERAGE.tsv, PROGRESS/DEFECTS and the final
exact-source evidence addendum before claiming Android/runtime completion.

Real normal command route is MainActivity.apply -> validated GameSession.legacy
supplier journal -> bind/autosave -> MapHost CombatSequence -> Filament effects.
Turn route remains TurnWork authority commit/autosave -> TurnPlayback display World.
Never restore presentation snapshots into authority. The retained completion ledger
is bounded and per-session; source IDs are transient and not persisted.

Long-press battle report banner accesses immediate playback controls. Any new
command, world install, background, 2D/3D or quality change cancels the old command
sequence and restores its committed snapshot. Existing turn pause/skip/critical
feedback remains separate; skip never cancels rule computation.

R10 actual run failed after legal auto-garrison at UnitR10Fixture.actor. Probe source
corrected but old run stays FAIL. Full core AI deployment and R09 PC-art/overlay/
modal defects remain inherited; no unrelated gameplay fixes in this visual stage.

Evidence must name full actual production commit built into BuildConfig, APK hash,
ABI/signer/backend, installed log and raw captures. Transport/workflow-only commits
are not the production source. Final docs-only HEAD can be newer than source.
