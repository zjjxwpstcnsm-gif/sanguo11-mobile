> 2026-09-25 整改复验：本阶段整体仍 **PARTIAL**。当前 APK 源码 `96719e971d74aa2b3f18d66a7f23832148a6dc8b`；当前关闭项、失败、设备与续作入口见 [R00–R14整改报告](../reports/R00-R14-REMEDIATION.md) 和 [逐条清单](../evidence/R00-R14-remediation-checklist.json)。下文原阶段记录保留，不能代替新版完整验收。

# R13 — editor, custom maps and save integration

Status: PARTIAL. Final installed outcome and exact APK identity are recorded below.

Baseline main ac29b458325b52d6e302ca44270d16552de4ed7f includes merged architecture
PR66; serial predecessor 0fdd96fe18a70a5617a250a4bb31650e7cae0705. PR67 remains
open/draft, targeting main. No AGENTS.md was present. Existing Filament1.56/OpenGL,
GameSession authority, map/AI rules, official resources and SaveCodec33 are retained.

## Production changes
- Edit persistence failure rolls back detached draft/World plus complete undo/redo
  history, byte accounting, saved status and generation. Success uses existing
  AtomicFile and refreshes normal MapHost; no Surface hide/recreate on every edit.
- Native editor selection uses the terrain picker rather than building model hits;
  source labels include actual national/cropped source coordinates.
- Damaged optional visual sidecar falls back to the verified pinned map or defaults.
  Optional style-save failure no longer prevents durable campaign saving.
- Map editor accepts only system-document content URIs. Existing bounded, versioned
  MapPatch and visual fields reused; no pointless new profile schema/editor.

See R13_FORMAT.md for format, ownership, halo reuse and SAF export limitations.
Assets are unchanged/transitional, not newly certified PC art.

## Host validation
Local production interaction32, custom-map136, R13 68 checks (including source
label assertion), brush4137372, durable ledger16, added-site gameplay12, custom
officers461, officer archive188 and architecture boundary PASS. Exact-source CI also passed R12 395124 and architecture1673 / coordinates32;
these counts describe host contracts, not mobile performance.
One-cell water edit retains38/42 streamed chunks; cached geometry/material/shore
attributes equal a clean build. Four yaw by three tilt brush picks tested. Fault
injection covers failed paint and failed undo, unchanged full saves and redo history.
Existing editor tests exercise supported scenarios, real new city/port/gate behavior,
port embarking, conquest, AI, old pinned custom saves and malformed imports.

## Runtime scope and inherited gates
Final source candidate a9d444e13332c6f0103c664d89db56070c27a273, CI36059464649.
The probe uses the real MapEditorActivity/change transaction, MapLibrary and normal
MainActivity.startScenario. Rotated brush uses MotionEvents; site CRUD invokes
production editor callbacks, not an all-touch dialog matrix. Imported city/port/gate
additions are labelled test fixtures, not changes to the official map.

R12 large opening-preview readiness failure remains open; no timeout extension or
assertion removed. R09 V2/PC comparison remains unaccepted. Full all-kind CRUD via
native touch, continuous undo/redo across every tool, manual SAF/cold-process restore,
portrait import UI, arbitrary cache dependencies and physical Adreno/Mali matrix are
not automatically covered by host/API probes. Physical performance/thermal NOT_RUN.
Do not merge main or start R14. Any failed installed gate remains FAIL/NOT_RUN.

## Retained failed candidates / subsequent validation fixes
- run36055781244 /458b3bd: production compile succeeded, androidTest compile failed
  on Files.writeString. Replaced only test evidence writes with Android-compatible
  Files.write(bytes); no assertions removed.
- run36056289938 /97b522f: APK/androidTest/lint PASS. Old editor runner was not
  registered (AGP default runner replaces first manifest entry); existing
  unityBridgeProbe=false/mapEditorProbe=true selectors now register it explicitly.
- Same run's native rotated-touch assertion FAILED. Fixture switched from a still
  gliding 2D camera; new probe focuses in 3D and captures/checks the visible picked
  target before touch. The subsequent44d23db run passed that assertion. The original
  failure remains recorded separately.
- First native recording was collected before its writer finished; MP4 lacks moov.
  Raw file retained as INVALID_RECORDING. Collector now stops the loop, signals
  screenrecord and waits for it to close before pulling.
- e6845de run36057560555 superseded/cancelled by expanded all-three-kind installed
  CRUD checks; cancelled execution is not a pass. Final candidate44d23db runs
 36057855779. Production files since458b3bd are unchanged; test/CI fixes remain
  separately traceable. Final APK must still embed the final candidate SHA.

- run36057855779 /44d23db: APK/tests/lint and legacy MAP_EDITOR67 ANDROID PASS.
  Native brush/swapchain/undo/redo/2D parity/height/custom model and all-three-kind
  rename/move/delete/undo/redo, durable close and damaged-style fallback reached.
  Process then crashed because the probe retained a null MainActivity map before
  startScenario created it. Host binding moved after actual scenario activation;
  no production code or acceptance assertion changed. Recordings180.591s/104.757s
  are valid MP4; original log and screenshots are retained.
- a3e0023 contains the probe fix; no R13 run was triggered because the workflow was
  restricted to its own path. a9d444e expands triggers to production/probe changes
  and supplies the final exact-source build. Evidence-only docs do not rebuild.

## Visual inspection and honest limits
Actual44d23db composed editor brush/site captures show continuous terrain and custom
GLB sites. Coastline/water remains blocky, trees sparse and small, some portrait
controls crowded and status text lags during asynchronous work. A screenshot's
presence and GPU proxy assertions are not PC-style art approval. R09 remains open.
The R12 full-map opening-preview timeout was not retested/fixed by local editor
shots or direct startScenario; that separate large-view gate remains unresolved.

## Exact APK identity
Source: a9d444e13332c6f0103c664d89db56070c27a273 (verified inside classes.dex).
APK SHA256: ac99b7513d93ce3029e9c08090fba3037331016df15c69fcc1f18eb42ee17df9
Size: 37346041 bytes. Application game.sanguo.mobile.dev; versionCode96;
versionName0.96.0-native-r13; universal arm64-v8a/armeabi-v7a/x86/x86_64.
Filament1.56/OpenGL, no libunity.so. Existing development certificate SHA256
8f64ee37f8ff58de8f5a199aac2ae745a5bc927d0d0eabac7540083a5e551f24.
Build: assembleDebug assembleDebugAndroidTest lintDebug with nativePcVisual=true,
unityBridgeProbe=false and mapEditorProbe=true. CI36059464649/job107834597744.
APK CI artifact10834007279 (14days, 2026-10-08 expiry). Source-matched prerelease:
https://github.com/zjjxwpstcnsm-gif/sanguo11-mobile/releases/tag/native-r13-a9d444e13332
Final report-only commit is an evidence-only HEAD; its runtime tree must match this source.

## Final installed outcome — scoped PASS, stage PARTIAL
CI36059464649 completed successfully; retained inherited core job is continue-on-error,
so green CI does NOT mean full core PASS. Raw interaction.txt: PASS R13 installed
checks=348. Legacy MAP_EDITOR67 ANDROID PASS (controls/gestures/library/recovery/new game).
API29 x86_64 Pixel2 / swangle. Actual rotated brush, same swapchain, undo/redo,
3D-to-2D parity, visual-height save invariance, imported city/port/gate GPU mapping,
all-kind production callback rename/move/delete/undo/redo, publish/close/reload draft,
corrupted style fallback, normal custom startScenario, deployment/movement, turn
playback, full SaveCodec reference equality, autosave and activation/load completed.
Checks include readiness polling; 348 is not a count of distinct user operations.
Native all-touch CRUD/SAF dialogs, cold process restart, portrait import and physical
GPU matrices remain NOT_RUN. Legacy fixture creation/import is not a native add-dialog
interaction. A saved draft read after closing is not an Activity cold-reopen test.

Five original MP4 segments are valid per ffprobe: 176.646,181.961,176.297,176.545,
7.359seconds. They are raw segmented screenrecord; not a seamless edited film or a
full-frame manual review. Raw UI + PixelCopy captures and renderer diagnostics are
included. Source/APK identity is above; evidence artifact10834980022 expires2026-10-08.

Performance NOT_ACCEPTED: actual Turn52 logs report total137728ms, compute132585ms,
save425ms,28factions. Software-emulator turn latency exceeds the existing target;
no physical GPU/thermal/long-duration claims. Do not interpret callback intervals or
CPU submission percentiles as measured GPU FPS. Root cause/regression attribution
of this latency is not established. Gameplay/AI logic was not changed to make it pass.
Final composed after-turn image still reads250年1月上旬 and retains an earlier status
clock; save/reference equality passed, but the probe does not assert calendar-widget
advancement. UI freshness/calendar presentation requires manual investigation;
do not declare the full visible turn UI accepted based on that image.

R13 delivery is PARTIAL: runtime transaction/data probe PASS; PC art/physical/manual
acceptance and inherited R12 preview/core gates remain open. No main merge, no R14.
