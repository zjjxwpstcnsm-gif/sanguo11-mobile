# 当前候选 v107 / P0 PARTIAL

APK source `a4b09058c6b1ab8c80f57d96f1e22425673de94c`。v106新增API29失败及全部对照保留在下文，不能当作已通过候选。

## 最终复验结论（2026-09-25）

**PARTIAL / 不可标为完整P0验收通过。** 已提交正式有界缓存、精确重复计算复用和全国景观合批；最终候选全国ready在两API原120秒限制内到达，未缩地图或改pending。原v105本轮也有通过样本，不能宣称102秒根因完全关闭。

| 最终候选检查 | API29 | API35 |
|---|---|---|
| 原完整NativeR12，CI36120063732 | PASS 60125机械checks | FAIL：installed next turn finished，120秒原断言 |
| 同套件正常冷启动 | PASS至真实新局109机械checks | PASS至真实新局68机械checks |
| 同一交付APK独立冷启动，CI36120858785 | 全国ready后FAIL：UI overlay continues drawing live labels | PASS至真实新局55机械checks |
| 日期原始整屏 | FAIL：七月/四月仍一月 | 七月正确；原v105也正确，非新修复 |
| 完整纯触控出征/移动/攻击/战报/下一旬/存读档 | NOT_RUN | NOT_RUN |
| ARM64真机 | NOT_RUN | NOT_RUN |

API35 R12先完成真实全国预览/确认开局，全国177块地形24,189.781ms（ground墙钟21,385.927ms、线程CPU2484.128ms）；其后命令链下一旬超时。日志Turn52仍逐势力执行，未证明与本轮渲染改动的因果关系，仍按最终候选新增失败保留，不能写继承失败或通过，也未改AI/延长超时。全国重复工作缺陷得到精确内容回归及实际ready支持，但全部P0没有关闭。

最终APK：version107 / 0.107.0-native-preview-cache，37370481 bytes，SHA256 `0ad4cc9b9e57a4535cddedf2939541a60433bc02c74d0703e6640696172da486`。完整source `a4b09058c6b1ab8c80f57d96f1e22425673de94c`。文档HEAD由交付REMOTE_DELIVERY.json及最终远端复读记录，避免自引用SHA。

原v105独立录像12/12、v106完整套件16/16、隔离v106及焦点配对7/7、独立v107冷启动2/2均作封装/全解码和逐段内容抽查。v107完整套件API29共8段，其中focused-window/remediation-2.mp4无有效duration、无可检查内容，**FAIL**；其余7段有内容，API35共9段有内容并全解码。部分录制有非单调DTS警告，保留原文件；旧v105损坏1段继续保留，采集链不能标完全修复。可解析及抽查不代表全流程或美术通过。

CI36120063732总体FAIL（API29运行机械门禁通过但Release403；API35原R12失败）；Release tag native-remediation-a4b09058c6b1复读404，不提供Release链接。独立APK、原始PNG/Surface/MP4/log/设备信息/退出码和输入交接通过独立文件交付。

续作入口：先复查API35 r12-full/logcat.txt的Turn52逐势力墙钟及SceneInstrumentation.commandFlow原120秒门禁；不改规则或AI。再沿API29停止的WindowFrames/HWUI与实际可见控件追UI合成，原APK无screenrecord也复现，不能归为纯采集或模拟器。最后再跑完整触控链、完整跨月跨年播放/生命周期矩阵。既有V1/V2、R10/R11/R13未闭合状态与213条要求全部保留。

# 历史：v105 后续前测与失败候选 v106

状态：PARTIAL，尚未关闭全部 P0。APK source `452f874144ad9893fdfce8188a375ee604c4f906`。
输入远端 `312532ea2a4d623f50c71becee119fbe06532d11`；main `ac29b458325b52d6e302ca44270d16552de4ed7f`。继续同一分支/PR67，无 main 合并、force push、其他 PR、R15 或规则修改。

## 原 APK 独立前测

CI36115487008 使用原 v105 APK 字节，SHA256 `2b38ca4741b60650be58555ec118350f2aeff292765f6b3754adc138c1fdf55c`；只构建原 instrumentation。生产修改前保存原始 PNG/MP4/logcat/退出码。

- API29：focused exit0（机械断言不代表日期画面正确）、原完整 NativeR12 exit0、冷启动 exit1。全国预览本轮未重现原超时：首地形177块55,809ms，景观788块合计60,458ms，pending最终0；首CPU epoch1/coarse断言到达，通过38949格/outside0，随后失败于 `UI overlay continues drawing live labels`。WAITING_FRAME 时 submitted13，采样条件要求20次提交，不能说没有3D帧。
- API35：focused、原完整 NativeR12、冷启动至新局均exit0。这次未修改APK本身首次切3D成功，不能据此声称修复上一轮失败。
- API29七月实际整屏仍一月；控件值、权威与snapshot不能代替整屏验收。HWUI记录dequeueBuffer -110，但未证明其根因，未直接归因模拟器。
- v105旧一轮API29 102,327ms与pending473/494失败继续保留；本轮55,809ms证明软件模拟器墙钟波动，不能把原总时长全部归为纯CPU，更不能外推ARM64手机。
- 本轮原APK12段录像ffprobe均可解析；旧v105包损坏一段继续保留。逐段首中尾内容抽查另附，封装有效不代表操作或美术通过。

## 正式运行路径修复

ScenarioFactionPicker → MapHost → FilamentMapView → 原SceneWorkQueue → loadVisible → Filament/Surface，未新造加载器。

1. TerrainSurface新增固定512KiB、原子key/value的直接映射四分之一格缓存。原半格缓存保留。原法线±0.25采样绕过半格缓存，重复执行邻域计算；现在有限碰撞只覆盖单槽，保持相同求值器和全部精度。
2. TerrainMaterialField每chunk最多4096坐标暂存，重复网格顶点复用原权重/法线/岸线，水陆flow通道分别保留，chunk结束释放。
3. 全国span≥40景观改16×16合批，近中景仍8×8；所有远景三角形不变。不解码/构造当前不使用的近景模型与道路。不同批尺寸切换时保留旧GPU块直至所有重叠的可见替代块上传，避免合批/拆批空洞。
4. 保持一运行/一等待/一结果队列、epoch丢弃、取消、owner线程、8次/4ms上传、原120秒ready及全部pending条件。地形与景观仍在同一结果交付；此轮先降低前置计算和批数，不声称已分离结果交付。
5. 正式诊断记录队列等待墙钟、backdrop/ground/scenery墙钟和线程CPU、几何/法线高度/混合/岸线段墙钟、进程GC与分配计数差、owner交付等待、代数/丢弃、帧回调/begin尝试及跳过/提交/成功PixelCopy。进程GC/分配不是该worker独占，分段墙钟仍含调度；几何段含顶点高度查询。

## 精确内容与规则保护

输入/候选主机完整core均exit1：`AI uses deployment commands / CoreTest.logistics:75`，不改AI或预期。R03和R08及受影响内容回归通过。

新NativePreviewWorkTest以未修改v105生成的黄金值核验184全国：177块地形全部vertices/indices/surfaceData字节SHA256 `e716c21a20f7043eed4ef2acaf6d77bc8c56a522a7fa61db65b89f6ab0a70b48`；236375景观三角形及UV/法线排序哈希 `6fd66849a26cc23b509b753745920f673f3f9dc2aa20313e4759f31c225a8275`。完整视窗主机景观950→247块；这不等于设备实际可见批数。并发高度精确值、同视窗引用复用、LOD合拆覆盖、完整SaveCodec/RNG不变通过。主机约2.26s→1.39s仅用于工作量回归，不是手机性能。

## 仍未完成

日期/播放陈旧尚无正式UI修复；跨月/跨年、结束/跳过、弹窗返回、后台/重建完整日期矩阵未通过。完整纯触控新局→出征/移动/攻击→战报→下一旬→手动保存/读档 NOT_RUN；原R12含API调用，不能替代。2D/3D×正常/关闭/跳过完整命令/RNG权威矩阵、20轮安装应用生命周期、快速换剧本/存档与过期worker设备矩阵 NOT_RUN。无可用ARM64真机；V1/V2、R10/R11/R13仍保留未闭合项。

## 候选设备、APK与制品

CI36117420833，最终失败结果见下文。构建命令：`./gradlew --no-daemon :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug -PnativePcVisual=true -PunityBridgeProbe=false -PmapEditorProbe=true`。

APK独立交付选API29构建：version106 / `0.106.0-native-preview-work`，37370477字节，SHA256 `5a25e4a789ef46b6171dd35a3e1fd7aecefac1c97392f26bbc461113c93c0dba`。应用ID `game.sanguo.mobile.dev`，ABI arm64-v8a/armeabi-v7a/x86/x86_64；开发证书SHA256 `8f64ee37f8ff58de8f5a199aac2ae745a5bc927d0d0eabac7540083a5e551f24`。独立检查APK中298份资产与source完全一致。

API35同source独立构建APK的整包SHA256为 `e00d5a88fc7b1c02d1a86d164728bda9c550d00d4e1c01fa7b1fa26741b372bf`；两个APK解压后的每一个ZIP条目字节均一致，包括classes/资源/原生库。保留两份整包身份，不能称两个ZIP整包哈希相同。16组主机CI全部exit0；两份完整core仍exit1。

### 日期采集独立控制

CI36118135967使用同一原v105字节、相同NativeAuditInstrumentation/API29配置，仅不启动screenrecord进程。原始独立screencap七月/四月仍显示一月；uiDraws9、WindowFrames8停止，Window PixelCopy=-2。机械PASS AUDIT仍非像素PASS。录屏不是故障出现的必要条件，不能称纯录像采集故障；未证明模拟器专属，不做该归因。原独立PNG、3D Surface、窗口与SurfaceFlinger和logcat已保留。

## v106 最终失败记录与 v107 续作

v106不能作为关闭P0的候选：CI36117420833 API29 focused0/R12=1/cold=1，后两者在120秒时cpuChunks0/worker_pending1/worker_waiting0/discarded0，尚未交付全国CPU结果。R12 frameCallbacks386/beginAttempts382/beginSkipped380/submitted2/surfaceCopies0；冷启动481/463/461/2/0。WAITING_FRAME确有两次提交，但没有完成内容采样，不能误称无回调或已呈现全国3D。首epoch断言未到达。独立冷启动CI36119354028和焦点等待后的CI36119737791在API29重复同一CPU超时，不能以一次环境波动消解新增失败。

API35 v106 原完整R12通过60004 checks；原冷启动在首次切3D处失败。录像10秒仍读取剧本、12秒窗口切换，未初始化Filament。独立冷启动CI36119354028已经到达epoch1、coarse、38949格/outside0，选势力后的截图为null而严格失败；不跳过采集断言。日期整屏七月正确，但原v105本来正确；API29整屏仍一月。

API35 R12全国地形177块：queueWait179.004ms，backdrop墙钟2614.334ms/线程CPU167.493ms，ground墙钟19545.662ms/线程CPU2624.839ms；景观211块墙钟4322.273ms/线程CPU362.673ms，进程GC计数差12736ms、分配计数差555549888bytes，owner交付等待12.114ms。GC为进程总计且可能与worker并行，不能与各段相加或归为worker独占；墙钟明显不是纯CPU。v106 Release发布仍HTTP403，未生成Release。

v107 source `a4b09058c6b1ab8c80f57d96f1e22425673de94c`，CI36120063732：进一步移除v106装箱Long坐标表，用固定4096槽long键/int顶点索引（48KiB），冲突单槽覆盖且精确键比较；稀疏采样组件时钟，避免每顶点多个nanoTime。增加每32块worker进度/线程CPU记录。主机全几何/合批/并发/权威回归再次通过，177/247/236375一致，1.20秒仅为主机计时。新增开销各自占比尚未在API29完整分离，不预判单一根因；设备最终结果见本报告顶部及末尾。

焦点触控测试修正 source `a683451478d3a80589df7f0b6e6bbc7b62587e1b`：await只选择attached且有焦点、已布局的真实窗口；触点必须两次屏幕坐标相同、控件启用且仍拥有焦点。仍用sendPointerSync真实down/up，不用performClick、不延长原30秒控制等待或120秒ready、不改结果断言。CI36119737791同时安装原v105和原v106字节对照，不能以instrumentation编译版本冒充APK source。

CI36119737791焦点触点对照已完成：原v105/API35与v106/API35均PASS至实际新局，原始完整整屏/Surface/录像保留；两者均epoch1/coarse/outside0，所有触点记录focused=true、稳定屏幕坐标。APK源码由安装前字节哈希和runtime `source=`证明；末行 `source=a683...` 是instrumentation内编译常量，不能误记为APK source。API29 v106仍CPU超时，此测试改动没有绕过运行失败。旧isShown时序存在缺口得到对照支持，但此前间歇失败全部原因未穷尽，不声称生产点击逻辑已修。完整开局后的出征/攻击/存读档仍NOT_RUN。

### v107 独立API29冷启动：全国ready到达，随后UI仍FAIL

CI36120858785使用交付同一APK字节，触点稳定等待与定位共用原30秒deadline，原120秒ready不改。全国177块地形/211景观批次完成，pending0/worker0/queue0/discarded0、GPU地形177，epoch1/coarse断言真正到达，38949格/outside0。原始整屏与独立3D Surface均有完整全国内容。随后严格失败于UI overlay continues drawing live labels，尚未选择势力；不能写完整冷启动PASS。

本次：queueWait0.363ms；backdrop墙钟835.656ms/线程CPU252.602ms；ground墙钟43687.582ms/线程CPU3758.218ms；scenery墙钟9106.791ms/线程CPU648.868ms；组合结果53630.029ms；进程GC计数差24446ms、分配574988600bytes；owner交付等待168.426ms。原v105本轮55,809/60,458ms，旧v105102,327ms；不能把不同运行墙钟当严格设备benchmark。累计的sampledNormalHeight/Blend/Shore仅634个稀疏样本墙钟，含调度/GC，不能当全部类别耗时或外推纯CPU。

提交前进：frameCallbacks276、beginAttempts269、beginSkipped259、submitted10。此时WAITING_FRAME是尚未达到原20次提交的内部采样前提，surfaceCopies0；独立Surface截图已成功，不把overlay270次当3D呈现证明。景观可见161批（原v105同视窗507批），primitives647（原993），triangles598577、buffers59173932完全一致，地形/玩法对象未删除。稳定首任务没有取消重排或重复全国任务证据，后续换档/生命周期完整矩阵仍NOT_RUN。

备用分配实验只在scratch运行，未接入正式源码：材质邻域Hex查询替为等价BitSet后主机分配339496712→198248728bytes、黄金内容通过；此项不是v107已交付修复，后续若需继续降低GC可从该诊断进入，不把主机结果冒充设备通过。

同一交付APK在CI36120858785 API35通过至真实新局（55机械checks，非55个触点），原图已复核。首地形177块16,250.707ms、ground线程CPU2019.296ms，景观211批，总CPU任务20,014.223ms；epoch1/coarse/outside0。API29全国上传阶段pending观测321→295→263→226→192→152→120→80→48→20→0且维持0，没有上传后立即驱逐证据。全阶段pending不是单一工作量单位：worker阶段1表示一个未完成任务，结果交付后转为缺失GPU批次数；不把阶段展开视为反复请求，也不掩盖这一非单调定义。

v107 APK37370481bytes，SHA256 `0ad4cc9b9e57a4535cddedf2939541a60433bc02c74d0703e6640696172da486`；API35构建整包SHA256 `5e3e4f606250beb8db8e4fe1b0d5b9fd774ce788c0612df4362de86815997262`，解压后的所有条目完全一致。独立冷启动两API均安装前者。298生产资产再核验全同；应用ID、四ABI、开发证书未变。

### v107 原完整套件 API29

CI36120063732：focused=0、原完整NativeR12=0（60125机械checks）、正常冷启动探针=0（109机械checks，已真实确认新局）。R12全国177块49,768.556ms、组合结果58,496.513ms，ground线程CPU4241.380ms、scenery线程CPU627.427ms；冷启动地形46,002.250ms、组合54,300.280ms，ground线程CPU3824.571ms、scenery线程CPU645.101ms，epoch1/coarse/outside0到达。

这不覆盖独立冷启动CI36120858785的UI停绘FAIL，不能只取有利一次。focused实际七月/四月整屏仍一月：uiDraws14、WindowFrames13固定、WindowPixelCopy=-2，控件和权威值已变。机械PASS不是UI日期/播放PASS，新局截图也不能代替跨月矩阵。全国ready在原超时内到达得到两条最终候选API29路径支持；整体P0/UI稳定性和完整触控链仍PARTIAL。
