# v112 P0 续作：PARTIAL，运行闭环尚未全部关闭

APK source：`5f8997ebfef15f4680931d40532411a336506d0d`。接手时实时分支为 `d7fae5d7e680d519b70a707df2f558a8507b6079`，继承 v108–v111 全部正式修改；构建/完整 core 输入为仅修 CI 的 `8b42af2a70c0fc066620e2ce79951aa31d8b923e`。最终远端文档 HEAD 在独立交付 JSON 中记录，不能混作 APK source。

继续同一分支和 Draft PR67；main 为 `ac29b458325b52d6e302ca44270d16552de4ed7f`。未合 main、未 force push、未操作其他 PR、未开始 R15。保留 Filament、MapHost/SceneRenderGate、相机初始化顺序、已合架构、有界队列/缓存。相对接手输入，core/game-api/game-runtime/data/unity、全部生产资产和材质生产源均无变化。

## 本次正式修改与边界

`FilamentMapView.doFrame` 现在在资产同步前统一选择 site/unit LOD。旧路径先按默认中景 LOD 请求资产，再在 `loadVisible` 改成全国远景 LOD 并再次同步；单位 LOD 甚至在该同步之后才更新。新路径仍在 owner 线程、成功 `beginFrame` 内运行，LOD 变化只设置原 `assetSyncPending`，沿用原解码队列和上传预算。没有加大上传量，没有删玩法对象，也没有预热/缩图/强制 READY。

执行提取的真实 `doFrame` 与真实 LOD 选择函数：输入失败于 `FIRST_ASSET_LOD: national first request must use far site and unit assets`；候选通过全国首请求、近景首请求、质量下限、滞回、拒绝帧无 GPU 写入、异常 endFrame 闭合。该 HOST 回归证明顺序修复，不等同全国预览安装验收。

`NativeColdStartInstrumentation` 增加首个 CPU 交付观察；观察调用位于原 `ready()` 的 120 秒循环内部，不新增等待预算、不替换真实触点、不减少旧断言。保留完成后的全国38949格投影、epoch/coarse、帧提交平衡和 live-overlay 检查。原完整 NativeR12 没有改动。

图形后端对照工作流先后两次没有到达应用：首次错误采集命令 exit127；第二次原始 stderr 明确为 `libpulse.so.0` 缺失，已经改为读取安装包 `source.properties`，不再启动另一个 QEMU 进程只为读取版本。两个失败留档，不算原 APK 复现。

## 原始 APK 与继承候选证据

- 独立重跑未修改 v105，SHA256 `2b38ca4741b60650be58555ec118350f2aeff292765f6b3754adc138c1fdf55c`：CI36208888111 本次 raw artifact10896891481。API29 正常冷启动经真实触点切到3D，177块地形耗时69564ms；最后 pending0、GPU177、epoch1/coarse、全国有效格38949/outside0。原整屏确有全国3D地图，随后严格 live-overlay 断言失败；HWUI记录 `dequeueBuffer -110`。本次没有重现102秒，不能据历史单次墙钟认定全部为纯CPU。
- 原 v105 Window 因果对照 CI36210927099：确认 backing 缺失后，一次诊断性同 Window 属性重送曾恢复 PixelCopy；之后四月加载再次失去 backing，整屏仍七月。此干预不是候选通过证据，也不是纯采集故障证明。
- 继承 v111 `107a8e6a3e6ba8bb072556198f374b202c2c85dc`，CI36212043685：API29 focused机械PASS181，但原始四月整屏仍一月，实际日期 FAIL。API29 R12 全国 pending41/提交39/尝试690/拒绝651；独立冷启动 pending204/提交19/尝试565。API35 R12 pending4/提交7/尝试626；独立冷启动 pending324/提交2/尝试436。不得把 Window 恢复计数或 overlayDraws 当实际3D输出。
- v111 API35 精确权威矩阵机械PASS：12组合、378checks（4/12月 × 2D/3D × normal/off/skip），比较完整 SaveCodec 字节及规则 RNG，并实际跨月/跨年及两种模式重建。是显式 fixture/API 命令，不是纯触控新开局；不是本次 v112 结果。
- 修正采集后的软件图形对照 CI36213832901：v105 API29 CPU结果尚未交付即超时；v110 API29实际 Surface PixelCopy status3；v110 API35发生真实安全回退；v105 API35没找到启动页控件（原录像可见System UI ANR弹窗挡在前台）。四项均 FAIL，没有一个可用来宣称换采集/换后端即修复。

## 全国预览五个问题的当前证据

1. v105 原墙钟约102秒不是纯 CPU 结论。本次原版测得69.564秒，未植入分项计时；不能从候选计时倒推原版成本。继承候选已有 worker 排队、backdrop/ground/scenery 墙钟与 thread CPU、网格生成、稀疏高度/法线/混合/岸线样本计时，以及进程级 GC/分配计数。稀疏样本不是全量各类计算总耗时，进程GC不是该worker独占时间，剩余差值包括调度/竞争/结果背压，不能全归为GC。
2. 继承的 phased mailbox 已每8块交付首次地形，全部地形先于景观交付；仍使用一个有界生产者和单槽交付。首批地形不再被全部景观生成阻塞。本次修掉额外的错误LOD首请求；地形纹理/图集仍有owner CPU工作，完整异步/优先级矩阵未闭合。
3. 继承样本中 epoch1、discarded0、worker_pending0，没有该次反复取消/失效重算的证据；不外推所有快速换档场景。初始资产LOD重复请求有源码+前后回归证明。全生命周期驱逐/上传回调仍需设备证据。
4. 回调、beginAttempts、beginSkipped、提交、Window帧和Surface PixelCopy单列。v111 API35独立冷启动436次尝试只提交2帧，CPU177块已齐全仍pending324；瓶颈已不能仅用地形CPU解释。没有以overlay绘制次数证明3D已呈现。
5. pending在分批CPU结果到达时可增加，因此不保证加载开始即单调下降；生产者结束后才应收敛。候选根据真正可见的未上传/待换材质网格重算，不漏计。`WAITING_FRAME` 在当前实现还受最少20次提交、pending0、非空可见块和有效Surface条件控制；它不等于绝对没有任何像素。原v105本次实际Surface已有地图却仍是WAITING_FRAME即为反例。严格真实Surface采集和原整屏必须独立判读。

## v112 独立冷启动的分项观测（软件模拟器）

| 指标 | API29 | API35 |
|---|---:|---:|
| 首CPU交付 | epoch1/coarse，已到达 | epoch1/coarse，已到达 |
| Ground完成墙钟ms（含backdrop） | 40369.868 | 23979.524 |
| 排队墙钟ms | 2.736 | 2.587 |
| backdrop墙钟 / thread CPU ms | 706.168 / 254.389 | 2185.043 / 299.388 |
| ground墙钟 / thread CPU ms | 39663.700 / 3631.488 | 21794.481 / 2276.361 |
| geometry墙钟ms（包含调度） | 7643.721 | 3689.584 |
| 稀疏高度法线 / blend / shore样本墙钟ms | 382.010 / 181.309 / 330.387 | 9.590 / 13.339 / 1.164 |
| 复用field样本 / 实际计时样本 | 179633 / 634 | 179633 / 634 |
| scenery墙钟 / thread CPU ms | 9853.496 / 634.498 | 3510.175 / 452.629 |
| terrain最终交付背压墙钟ms | 106.297 | 81.247 |
| 单槽累计背压墙钟ms | 322.752 | 2919.226 |
| 进程GC增量ms（非worker独占） | 15024 | 6000 |
| 进程分配增量bytes（非resident） | 367547456 | 479876480 |
| CPU地形 / 景观批次 | 177 / 211 | 177 / 211 |
| begin尝试 / 拒绝 / 提交 | 468 / 466 / 2 | 441 / 436 / 5 |
| 超时时pending / GPU地形 | 322 / 16 | 307 / 31 |
| asset CPU缓存bytes | 231704 | 231704 |
| worker_pending / discarded | 0 / 0 | 0 / 0 |

数据来自同一v112 APK的独立pm-clear冷启动，不是手机性能结论。v111独立冷启动asset缓存642384bytes；v112移除错误中景首请求后231704bytes。CPU队列已交付完毕而GPU仍未收敛，仍需原生driver线程/帧fence与Surface输出定位；现有证据不支持宣称全部根因已经关闭。

## v112 身份与构建

- 版本112 / `0.112.0-native-first-asset-lod`；应用ID `game.sanguo.mobile.dev`。
- 独立APK SHA256：`6ba2211d961567b2aa396ba19e3018c1dc44780fa4351a807db099fc643bd058`，37387038 bytes。
- ABI：arm64-v8a、armeabi-v7a、x86、x86_64。
- 开发证书SHA256：`8f64ee37f8ff58de8f5a199aac2ae745a5bc927d0d0eabac7540083a5e551f24`。
- 构建：`./gradlew --no-daemon :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug -PnativePcVisual=true -PunityBridgeProbe=false -PmapEditorProbe=true`。
- CI36213760665精确source构建成功；本地独立检查全部300项APK资产与source逐字节一致，没有Unity运行库。300项/6材质是继承v110远景材质后的保护清单，本次没有再改资产；不能误写回v105的298项。
- Release实际HTTP403，查tag失败；不提供虚假Release链接。独立APK与CI artifact10896507757可交付。

## 本次验证结果

| 范围 | v112 实际结果 |
|---|---|
| 输入/候选真实首资产LOD回归 | 输入FAIL、候选PASS；两API安装首CPU与farLOD检查均到达 |
| API29 独立正常冷启动 | FAIL，全国ready120秒超时，pending322、提交2；真实点击3D回调已到达 |
| API35 独立正常冷启动 | FAIL，全国ready120秒超时，pending307、提交5；真实点击3D回调已到达 |
| API29 原完整NativeR12 | FAIL，初始scene ready即失败（pending32、提交4），尚未到全国预览 |
| API29 日期专用探针 | 初始ready失败，月份加载检查NOT_REACHED；继承v111整屏日期FAIL仍未关闭 |
| API35 原完整NativeR12 | FAIL于recreate:22 / combinations:32 / onStart:91；pending5、提交9、尝试609/拒绝600，尚未到全国预览 |
| API35 focused日期 | 机械PASS186，原整屏一月/七月/四月正确；旧未修改APK在API35也正确，不能算API29日期故障修复 |
| API35 完整权威一致性 | PASS12组合/310checks；50对完整SaveCodec字节独立复核一致，包含RNG、攻击提交/结束、下一旬及两次重建；显式fixture/API操作，不是纯触控 |
| API35 20轮生命周期 | job108325858061截至最终快照仍in_progress，完整安装验收NOT_RUN |
| 完整纯触控新开局链 | 未通过。势力确认、出征、移动攻击、战报、下一旬、手动存读均NOT_REACHED |
| ARM64真机 | 不可用，NOT_RUN；以上计时仅软件模拟器 |

完整core输入/候选均保留原失败：入口exit1，`AI uses deployment commands / CoreTest.logistics:75`。收集全部42个原始调用，输入和候选均12PASS/30FAIL，退出矩阵一致；不是只有一个失败，也没有改AI或预期冲绿。20个主机套件exit0，包括新首LOD回归和现有流式交付/窗口模型；HOST通过不代表安装通过。

候选已收集17段原MP4，均ffprobe与完整解码exit0，并逐段检查开头/中间/结尾内容：冷启动/R12仍加载失败，API35 focused与parity已到达日期、命令结果及重建。直接检查API35原整屏一月/七月/四月及代表性跨月、跨年、重建后的PNG正确。另复核原v105、窗口因果对照、v111相关运行和软件后端对照，共36段。保留原整屏PNG、3D原图（探针到达时）、原视频、logcat、交互输出、退出码和设备属性；候选未到实际Surface截图的步骤明确缺失，不能用overlay或截图裁片代替。历史v105坏录像仍为已知缺口，本轮有效录像不等于历史采集链已全部修复。

最终仍运行的20轮生命周期CI不是完成证明。本报告不承诺会话外监控；续作应首先按job ID读取最终状态与原始制品，再补充对应验收。不要把in_progress、超时或无输出改为PASS。

## 后续入口

先继续全国实际帧阻塞：保留冷启动和原R12两个入口、原120秒，观察成功提交与驱动/Surface实际前进，不能继续只提高每帧上传量。然后处理 Window recovery 已确认loss但重送后仍AWAITING_BACKING的失败，逐次对照可见日期/播放及窗口帧。正常启动的首个CPU任务检查应在加载期间直接可见，不能藏在成功截图后面。

待全国预览稳定后再跑清空隔离数据的完整纯触控：新剧本→全国3D→势力→确认→选城出征→选军移动攻击→战报→下一旬→手动存读。原R12和parity中的API操作不得冒充该链。20次生命周期、快速换档/过期worker/资源释放未完成的项继续NOT_RUN。ARM64真机不可用，手机性能及30分钟热稳NOT_RUN。V1/V2与其余R10/R11/R13原缺口不升级；213项原文与历史状态保留。

CI：https://github.com/zjjxwpstcnsm-gif/sanguo11-mobile/actions/runs/36213760665

PR：https://github.com/zjjxwpstcnsm-gif/sanguo11-mobile/pull/67
