> 2026-09-25 整改复验：本阶段整体仍 **PARTIAL**。当前 APK 源码 `96719e971d74aa2b3f18d66a7f23832148a6dc8b`；当前关闭项、失败、设备与续作入口见 [R00–R14整改报告](../reports/R00-R14-REMEDIATION.md) 和 [逐条清单](../evidence/R00-R14-remediation-checklist.json)。下文原阶段记录保留，不能代替新版完整验收。

# R03 — constrained field and view-bounded terrain

Status PARTIAL. Continues R02 2965bcddd50564f80cb194aa71127767d822dd10,
main ac29b458325b52d6e302ca44270d16552de4ed7f (architecture PR66 integrated).
PR67 remains draft, main is not merged. Filament 1.56.0/OpenGL retained.

## Audit and production changes

Normal MainActivity → MapHost → FilamentMapView path now requests a bounded
terrain window with 16-unit prefetch and 12-unit movement slack. One cancellable
SceneWorkQueue holds requests; pending work completes before a changed camera
requests another window, so continuous dragging cannot starve completion.
Nearer chunks build first. GPU uploads retain the two-mesh/frame budget; existing
visible levels remain until replacement upload, then retire. This is a count
budget, NOT a measured 2ms limit. Camera-only updates retain the scenery GPU.

TerrainSurface v3 uses a compact Wendland kernel (radius 3.4) with zero derivative
at sample centres; the prior radial cubic kernel had a centre cusp. Hard water,
site and path constraints remain, but their release slope is now a 4.5-unit
smoothstep instead of a constant-gradient ramp. The minimum of constraint caps
can still form derivative creases; no claim of fully smooth shores. Existing
world ridge parameters (.16x+.11z+.7sin(.08z), height 1.6+.9ridge) remain reproducible.
Noise is not substituted for mountain design. Half-unit sample cache is bounded
at 32768 entries, clears on saturation, and does not affect deterministic values.

Three production LODs keep the same canonical fan planes and edge vertices:
8 / 24 / 72 triangles per logical cell. Interior planar subdivisions improve
material interpolation only, NOT terrain silhouette. All levels therefore have
zero anchor/pick displacement and need no skirt. Chunk-centre Chebyshev distance
bands of 32 units keep source-neighbor levels within one; span floors are 14/40.
Only the requested level is retained per chunk. At national overview many coarse
chunks are legitimate; it does not retain all national precision levels.

Production fingerprints include map identity, local terrain/site/paint halo,
layout and generator/material/water versions and LOD. They no longer include a
monotonically increasing global mapRevision, which had invalidated the whole map
on a local patch. Ground.matches still detects revision updates to request new
work. Legacy whole-map audit overload keeps its original seed-invalidation
contract. Vegetation retains its existing version-dependent seed contract; changing it
failed the independent FieldAssetsTest in CI35869690421. The new failure was
fixed in production without deleting/changing that assertion. Its dependency
halo expands to eight source cells, but it still retains whole-map CPU meshes
and reseeds on mapRevision: explicit remaining memory/incremental-update debt.

## Verification

Host R03: 12,368,081 assertions PASS; near home window 25 chunks; two grid layouts
single edit + revision increment rebuild 4/36 chunks and retain 32 identities;
all cached arrays equal clean rebuild. Checks indices, finiteness, shared
height/tangent/UV/material, real triangle pick oracle, all-level anchors and
neighbor level differences. National complete SaveCodec bytes unchanged:
SHA256 1092dd42e8ae7c6875853b5a097294694a7f5aba0986a8a58a255893360579e5.
R02 host: 399124 assertions PASS, 38949 national centres and 1548 camera rays.
Further regression/build/installed results are recorded after exact-source CI.

R02 precondition recovered: exact-source CI35865854066 concluded success,
artifacts native-r02-apk 10752528845 and native-r02-evidence 10752778375.
This supersedes its pending PR note, not the prior failed run. R02 report/handoff arrived in concurrent documentation commit 943ddcf, merged
intact into R03; its 1043 installed checks and remaining gates were reviewed.
No historical manual/device acceptance is invented.

## Remaining acceptance

Actual R03 Surface comparison, recordings and APK: pending candidate CI.
ARM64 Adreno/Mali and handset performance NOT_RUN; PC REFERENCE_MISSING.
Exact cell water silhouettes and hard-mask seams remain R05 debt. Existing
material/asset repetition remains; stage cannot be visual PASS based on math.
R01 decode/fault/device limitations and legacy failures stay in DEFECTS.md.
No R04 work started. No gameplay, authority, map data, RNG or save rules changed.

## Exact-source build and candidate history

Superseded inspected candidate: 38eddcf71bea94561fe7700ce1a444667bcb132f.
Build/lint/test-APK PASS in CI35871032226, artifact10755556339.
Universal APK: 35,388,742 bytes, application game.sanguo.mobile.dev,
version86 / 0.86.0-native-r03, ABI arm64-v8a/armeabi-v7a/x86/x86_64.
SHA256 80605f1348b865b0f126e74c8bc5488ffc5e30da368f3138925c204e0c049933.
Development certificate SHA256
8f64ee37f8ff58de8f5a199aac2ae745a5bc927d0d0eabac7540083a5e551f24.
Exact source independently found in dex; no libunity.so. Build command:
./gradlew --no-daemon :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug -PnativePcVisual=true.

c986822 compiled but introduced vegetation version-contract regression. f8bbcb0
restored the production seed; existing field/environment host suites then PASS.
f8bbcb0 also built successfully, but inherited S10/S11/S12 instrumentation reflected
a removed diagnostic field and exited before the shot matrix. 38eddcf restores
that field as a truthful all-accepted-chunks-at-coarse summary, also in diagnostics.
No assertion was deleted/relaxed, and earlier APKs are not final deliverables.
Superseded R03 runs were cancelled by the existing workflow concurrency policy.

Extra host checks: nationwide S13 33,722,067 assertions PASS; synthetic painted
peak ray foreground occlusion PASS (280 samples); shipped 8 terrain textures,
compiled ground/water material and 175 environment hashes PASS. CoreTest exits1
at AI uses deployment commands on both baseline943ddcf and candidate; raw logs
retained. core/game-api/game-runtime/map/assets are unchanged by this stage.
Local Gradle download is blocked by Network unreachable, so builds are CI-only.

CI35871032226 completed PASS with 407 installed checks; R00 CI35871032368 also
PASS. However manual screenshot review found the initial home focus at
(67.75,54.5), not its intended map coordinate (129,52.5). The old clamp used
loaded chunks as world bounds. This is a NEW R03 regression despite test success.
Ground now stores full-map bounds, SceneCamera clamps to those immutable bounds,
and new all-site host / installed initial-focus assertions guard it. The prior
candidate is retained only as failure evidence; a new APK must be built.

Host after focus fix: R03 12,368,259 assertions PASS. Corrective bounds do not
change terrain geometry, map/rules or the LOD/picker surface.

67d8d05 focus check passed (129,54.5 is the initial home), and all terrain views
were captured, but CI35872850213 failed the full turn-byte equality assertion.
Inspection found the shared probe reads MainActivity.world off-owner immediately
after aiRunning becomes false; completeTurnPlayback clears that flag before
bindSession. The probe now captures world through runOnMainSync after the callback,
retains the full equality assertion, and exports both states if it fails again.
R03 drag events now allow 40ms/frame-loop opportunities between events instead of
dispatching every event in a single main-thread callback. Production source is
unchanged from67d8d05; exact test-source APK is rebuilt anyway.
