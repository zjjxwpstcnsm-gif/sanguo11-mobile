# R05 — constrained bank / water candidate

Status PARTIAL; only R05 is authorized. Continue branch agent/native-pc-visual,
Draft PR67; no merge or Unity restart. Entry: MainActivity → MapHost → FilamentMapView.
Backend remains Filament 1.56.0 / OpenGL.

## Audit and actual changes

Initial main ac29b458 includes architecture PR66; initial serial HEAD b78566b.
R04 final CI35940507513 was read live and completed successfully. R04 handoff was
absent at initial HEAD; any later upstream documentation is retained.
GameSession, core rules, map bytes, six-neighbor navigation, port positions and
SaveCodec remain unchanged. Existing immutable Ground drives all water batches.

Water material now borrows the four existing sRGB ground albedos (no extra texture
uploads), using identical world UV, two scales, weights, macro tone and wet-bank
color at the contact. Opaque textured bed → muted sediment/shallow → deep green
water transitions replace the abrupt unrelated bank color. Waves vanish at the
contact and deep-water roughness is increased to reduce plastic highlights.
No transparent sorting, refraction or reflection framebuffer added.

Near/mid mesh subdivision now samples actual signed shoreline distance and local
flow at NEW interior vertices instead of merely interpolating coarse values.
Existing boundary vertices and geometry remain unchanged: no self-intersection,
new winding, navigation shortcut or picking/boat/port anchor height changes.
The existing eight-cell halo and generation-checked worker reject old edit results;
no new global component-dependent cache or artificial river route is introduced.

## Verification

R05 host PASS 55,818 exhaustive-distance oracle, winding, protected centre,
interior-refinement and national port assertions. Existing S11 PASS 803,860:
components, exact mask/area, single-cell channels/island, edge/cross-chunk,
edit cached-vs-full rebuild and undo. R04 PASS126627; R03 PASS12368259;
architecture script passes. Material hashes and matc56 compilation pass.

Legacy PortReplayTest FAIL at aiDocks(false), trace [7,6, 10,5]. Independently
compiled/reran on untouched b78566b baseline with the identical failure.
This is retained as an inherited rule/test failure, not fixed by a rendering task;
CI runs it explicitly with continue-on-error so APK/graphics evidence still runs.
It is NOT a successful port AI acceptance result.

Exact-source Android build/lint/installed evidence will be recorded after CI.
Full installed embark/sail/landing recording, edited-map playthrough, matching PC
water reference, ARM64 phone/thermal/performance remain NOT_RUN.

## Limits

This is conservative material/bed contour refinement, NOT a new curved shoreline
mesh. The exact staircase silhouette may remain at narrow constraints and far LOD;
PC visual acceptance must not be marked PASS without actual comparison. No invented
hydrological direction: flow remains a local shape-derived visual field. Existing
lake/river water plane remains height zero, shared with all prior anchors; no new
height flattening is introduced. Existing building/vegetation art debt remains.
