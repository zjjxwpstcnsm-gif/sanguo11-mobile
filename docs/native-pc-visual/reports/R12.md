# R12 — native map interaction

Status: PARTIAL candidate. Continue agent/native-pc-visual / draft PR67; no main merge or R13.

## Baseline
Main ac29b458 includes architecture #66. R11 e581b270 retained. R11 run36019140571 is completed/success as re-read from GitHub; its candidate report has not been treated as final evidence. R09 V2/art, full manual interaction and physical Adreno/Mali remain unaccepted. No AGENTS.md exists in the checked-out tree.

## Production
- Actual Filament overview replaces the old fit-only navigator button. Cached north-up terrain/territory raster and live sites/units; tap/drag changes the same camera only. Viewport uses inverse camera plus terrain heights, including yaw. Toggle inherits the existing navigator preference and survives 2D/3D and camera restore.
- Territory preference moved to MapHost with one-time legacy Activity preference fallback. Preview's forced territory and editor overrides do not overwrite gameplay preference. Grid remains independently persisted. Temporary editor grid is labelled.
- Host projects immutable authority boundary masks; mode1 merges same-faction borders, mode2 shows actual jurisdiction. Fill alpha lowered from85 to48/255. Grid fades in distant views independently of tint and has no effect on picking/ranges.
- Terrain-sampled subdivided overlay edges; tactical x-ray policy: centre-obscured cells have dashed outlines and an explicit legend. This is Canvas annotation, not GPU depth-tested decal. It cannot change rule reachability. Partial occlusion within a single cell remains a limitation; ordinary grid/faction fill still project through hills.
- Ordinary attack targets now share the exact existing 2D authority query through MapSceneSnapshot; red outlines are restored in 3D without renderer range calculation.
- Explicit commandTargeting is forwarded from MainActivity; reachable selection alone no longer suppresses object picking.
- Stable-ID unit selection no longer falls through cell arbitration just because `moving` holds a selected friendly unit; only armed commands use target-cell dispatch. No automatic command or detail panel added.
- Panel/dock blank space consumes touches. Native labels clip to available map area and avoid the overview. Narrow layouts reserve first row for date/AP and put map toggles in a second row. Existing feature tree retained.

No core, game-api/runtime, topology, save format, RNG, shader or model assets changed. Terrain ranges still come from MapSceneSnapshot / rule-query sets, tactics/targets/routes from existing host callbacks.

## Validation scope
Host R12 tests compare official all-cell faction/site borders, navigator world roundtrips and unchanged full save; explicit fixtures compare move/facility ranges. Retained interaction/editor, R11 combat and architecture run unchanged in CI. Existing full core failure is retained separately. Installed R12 tests cover four switch combinations, actual Activity recreation, 2D parity, temporary editor grid, overview input with rotated camera, panel interception, scenario preview/start, official deployment/move/turn/autosave/load and explicit combat fixture. Surface and composed UI captures plus raw screen recordings are produced by the installed APK.

Exact source/build/runtime outcomes and hashes will be appended after CI inspection. No candidate test is labelled PASS in advance. Physical devices, full touch-only walkthrough, orientation/device-size matrix, long-run performance and matched PC art remain NOT_RUN/unaccepted.
