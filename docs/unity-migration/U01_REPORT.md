# U01 report — BLOCKED

- **Source commit / tree:** remote `faaa0e40ae6b1ab1c1fc8f26d6d1c32745b4a33c` / `fbcea99b89d420aeffe1c82cf93e21a4a5053be5`. This commit carries the code, Unity script and recorded scenario fixture; later report-only commits do not change the APK source. **Delivery commit for a Unity APK:** none, because no U01 Unity APK exists. Branch `agent/unity-pc-visual`; Draft PR #65 stays open and unmerged.
- **Actual implementation:** one process-level `UnityBridge` session bound to the existing `MainActivity` World; a full map/visible sites/units snapshot, bounded delta/event/receipt queue, session/revision/sequence/command ID checks, idempotent retry, rejection and explicit resync. Android handler executes existing `World.patrol` / `World.recruit` on its UI/logic thread. Native UI successful commands signal the same session. Unity script polls asynchronously, renders a diagnostic terrain view with true entity markers, and offers a real patrol command plus an intentionally invalid one. Editor uses a labeled read-only snapshot recorded from Java `coalition-190` with seed 20260923. See `BRIDGE_CONTRACT.md`.
- **Java test:** `bash scripts/test-unity-u01.sh` passed locally from this source tree. It compared full `SaveCodec` bytes after the same command on direct and bridge paths, covering saved RNG; it checked invalid, duplicate, stale and out-of-order commands, session rotation, bounded queue overflow and fixture reproducibility. The full UI presentation regression `scripts/test-ui-models.sh` passed using a temporary JDK compiler shim. `scripts/test-core.sh` still fails its inherited `CoreTest.logistics` assertion `AI uses deployment commands`.
- **Android / Unity / device:** `scripts/build-unity-u01.sh` was added, but its U00 preflight exits code 2 because the licensed Unity Editor and Android support are absent here. There is no U01 **Unity** APK, signer check, installed C#→JNI recording, screenshot or ARM64 performance sample. [Native host workflow run 35809725433](https://github.com/zjjxwpstcnsm-gif/sanguo11-mobile/actions/runs/35809725433) passed Java parity and Android APK/androidTest compilation from exact PR head `6685e703f28892dfec7ad6f4bea0cf92f8a812c0`; its emulator protocol probe remained in progress at report time. The separate native diagnostic APK is 35,355,962 bytes with SHA-256 `9b0ff7b3b9f036ff1b0777d4bd5b00dd035f8a74efc120fdc6a4d21bc07f1149`. Its artifact checksum and APK ZIP CRC passed, and inspection confirmed it does **not** contain `libunity.so`. It cannot stand in for the required Unity Player APK.
- **Version / target:** Unity project pins Editor 6000.3.16f1 and URP 17.3.0; Android identity `game.sanguo.mobile.dev`, versionCode 82, ARM64 Unity target; exported module, build size, signer fingerprint and actual ABIs unverified. Host counters for command count/mean microseconds and queue drops exist; GPU frame time, FPS, PSS, draw calls, triangles, loading peak and APK growth remain unmeasured. No original PC scene comparison or art approval is claimed for this infrastructure stage.
- **Five axes:** function PARTIAL; visual NOT_STARTED; performance BLOCKED; device BLOCKED; push COMPLETE for source commit (documentation follow-up requires final remote HEAD check). **Overall BLOCKED.**
- **Next gate:** provision authorized Unity 6.3.16f1 Android runner and named ARM64 device, close U00-01/U00-02, then compile and install from the recorded source tree, execute the real C#→JNI command/rejection cycle and 10 return cycles, record screen and memory/frame evidence. Only then close U01 and start U02.
