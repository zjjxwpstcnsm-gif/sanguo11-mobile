CI35082212654: new Android fixture failed save validation because ruler loyalty was 80. Fixed fixture loyalty to 100; no application validation or assertions removed. Both genuine v0.9 and v0.25 APK upgrade gates already passed in this run.
CI35083156071: portrait transport, grain consumption, filter/recreation and cross-district support completed. Next orientation failed because the test left the report dialog open. Added its Return action.
Screenshot review also exposed a report remaining stale after support confirmation. Application now refreshes actual district stock, convoy and budget; installed test requires 50/60 points on screen after the real dispatch.
Added 36-turn independent resource ledger and a final-ration UI receipt check; existing completion/cargo assertion retained.
Baseline probe failures and final pass outputs are preserved separately.
CI35083797170: the new anomaly filter correctly survived orientation/fixture replacement. The next independent no-anomaly fixture could not locate its city while still filtered. Test now explicitly selects All States through the real UI between independent scenarios; application restoration behavior remains intact.
Trajectory audit found the original weak-city test could finish with rear engines still waiting. All original weak-city assertions remain; added actual two-origin convergence and engine choke traversal. An additional 8000-defense fixture requires a real engine hit, since weak cities may fall to ranged troops earlier. Matching old-AI failure and new pass logs preserved as campaign-journey-before/after.txt.
