# v0.30 验证记录

运行代码提交：`da3f09f`。版本名 `0.30.0-geography-skills-dev`，versionCode 30，包名 `game.sanguo.mobile.dev`，最低Android 8.0。

## APK

- 文件：`sanguo11-mobile-v030.apk`
- 字节数：6,605,001
- SHA-256：`811b3c5688ffda9bc0ecf2fd7c671a9b0e68695645b228bffdaeb13eb71e2add`
- 开发证书SHA-256：`8f64ee37f8ff58de8f5a199aac2ae745a5bc927d0d0eabac7540083a5e551f24`
- 保持旧包名与开发签名。不要卸载旧应用来更新；直接覆盖安装。

## 已通过的本地检查

- 完整核心规则、旧存档和长局回归；新地图/开发地/火系组合1,391条断言。
- UI模型、相机、城市与势力颜色51,810条断言；内容19项测试和资源生成一致性检查。
- 最终Android APK与测试APK编译；Lint 0错误/24警告（主要为现有中文字符串拼接/多语言提示，以及两个自定义View构造提示）。
- 首轮API29、1080×1920原生触控：地块建造、草稿旋转恢复、分页恢复、取消拖动不消耗、拖动后可继续下令、存档恢复不重复执行均通过。
- 最终安装包API29、1080×1920触控复验通过：安装APK的SHA-256与交付文件一致；全国缩放数值断言、地块建造、旋转、分页恢复、拖动与取消均通过。原生日志见 `native-final.log`。
- 截图核对：曹操领土为蓝色，分页列表从原先半行增加到多张可读卡片。`cao-territory.png`是近景截图；模拟器在全国缩放检查后的抓屏仍呈现近景缓冲画面，因此不把该图当作全国全貌证据。`geography-data.png`为实际地形数据预览，非APK截图。

## 验证边界

当前运行环境没有KVM，使用Android x86_64软件模拟器。复跑先遇到残留AVD锁；后续一次启动在游戏运行前出现System UI无响应弹窗，最终使用只读AVD启动并在进游戏前点Wait恢复系统界面后通过。没有关闭游戏自身的错误弹窗或忽略失败断言。没有用其运行时长宣称真机性能提升。本轮尚未完成远程三屏完整CI或ARM实体手机验证。

自动审批拒绝将本轮源码推送到GitHub，理由是本轮未明确授权上传到推定的仓库；没有改用其他接口绕过。分支与提交已保留在本地，APK可直接交付。PR和远程构建链接尚未产生。

新地图与开发地需新开群英汇聚等三个大地图剧本；旧存档保留原地形与旧开发规则。地形为人工重建，地块容量已按城核对，具体格位及部分技能公式仍与原版有差异，详见[功能与来源说明](../../GEOGRAPHY_SKILLS_V0_30.md)。
