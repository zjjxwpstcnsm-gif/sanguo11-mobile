# v0.32 安装包与验收

运行代码 `aac1b8bde6c2b5fc456793dc131e94b3ff89689e`，基于实际交付 v0.31 的 `877b2a0`。本目录之后的提交仅补交付说明及验证证据。

## APK

- 文件：`sanguo11-mobile-v032-aac1b8b.apk`，7,084,466 字节。
- SHA-256：`c4387b66b9e443e7166133d5a34b944b4935d42653eba1f45df06f18fbe65cdf`。
- 版本：`0.32.0-navigation-defense-dev` / versionCode 32；包名 `game.sanguo.mobile.dev`。
- 最低 Android 8.0 / API26；target SDK35。
- 开发证书 SHA-256：`8f64ee37f8ff58de8f5a199aac2ae745a5bc927d0d0eabac7540083a5e551f24`，与已交付 v0.31 一致。
- 存档版本仍为22，读取v1–v21；本轮没有改变保存格式。新增地理与年代归属需新开局。
- APK 内30份核心资源与对应源码逐字节相同；身份信息见 `apk.json`。

## 核心与构建

- `scripts/test-core.sh` 全部通过，包含历史存档和36/72旬回归；新增导航/守备/年代开局1,142条断言、已有地图/开发地/火系1,391条断言。
- 新增六剧本各推进三旬，并逐旬与存档恢复后的镜像局面对比完整保存字节；日期、君主、主要人物年代转属、未来登场、初始同盟与地图站点均检查。
- 全国七份开局各核对42城、10关、35港；港口临水和两个陆路出口、全部开发地、北上连通与实际出征/行军均检查。
- 城防测试包含普攻、弩兵、冲车、井阑、投石，射程外不反击、围攻预算共享、停战同盟、断粮、空城、据点易主及击破清理；每旬仅结算一次。
- UI/相机51,810条断言、内容19项测试通过；两份剧本生成器 `--check` 通过。
- 主APK及测试APK编译通过；Lint 0错误、24警告。

## 安卓端操作

测试使用 API29 x86 软件模拟器、1080×1920，包含横竖屏。原生测试覆盖小地图点击/拖动且缩放不变，收起/展开，界面重建保留偏好，菜单分别切换主将和双条，港口/关卡显示，远景继续走位图绘制，以及190剧本选择曹操后新建并恢复。

既有原生操作回归也通过：开发地建造、重建恢复草稿且不重复扣费、670武将分页恢复、部队长按拖动/取消及移动后继续下达战斗命令，见 `native-fidelity.log`。

最终导航复测增加明确的近景倍率断言（至少全图倍率的8倍），并等待实际画面呈现后截图；目检确认主将姓名与两条不同颜色状态条可见，小地图跳转仍保持该近景倍率。

实际测试日志和 `v032-*.png` 截图随本目录保存。六个剧本的完整规则检查在核心测试；安卓正式新游戏入口抽查190，并使用250局面验证导航。

## 范围与限制

本轮地图坐标和剧本属于人工重建/定制，尚未完成官方全量归属、资源、事件和外交等价核验；细节见 `../../NAVIGATION_DEFENSE_V0_32.md`。守备伤害为有上下界的工程平衡参数，不宣称原版公式。

软件模拟器启动时出现过System UI无响应弹窗，进入游戏测试前已选择等待。没有ARM实体手机测试，不能将这里的通过状态解释为真机60/120 FPS或全部机型兼容承诺。签名和包名一致支持覆盖安装，但本轮未重新执行历史APK覆盖升级全套流程。

源码保存在本地 `agent/navigation-defense-scenarios-v032`，交接包包含可独立克隆的git bundle及工作树快照。此前上传被自动审批拒绝，本轮没有push、PR或远程CI；不以其他接口重试该操作。
