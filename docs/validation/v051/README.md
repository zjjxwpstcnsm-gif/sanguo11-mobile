# v0.51 验证与交付边界

基线为远端v50 `7a85a47cce2545a70b86d3c7e3b723bd8f3af0fc`。本地源包基线git对象与远端提交ID不同，但18项资源指纹与v50完全相同，远端构建必须从上述真实提交重放，不使用本地基线SHA当发布身份。

## 已执行的本地检查

- 近期核心回归 `scripts/test-personnel49.sh` 通过，保留港口AI/攻城、粮耗忠诚、沙地、生产产能、持续目标与位移等既有规则。不是完整历史全量测试。
- `MapFidelity50Test` 1,446项检查通过：九剧本、35港上下船、10关、原14条路线、建满开发位和地图保存读取。
- `MapFidelity51Test` 364项检查通过：七份全国剧本、11条新增验证的地面路线、建满591开发位、13个难所格无科技/有科技通行与保存读取。
- `test_geography_v051.py --base <本地精确v50基线>` 174,252项检查通过：九图接缝和全部非地理记录不变、1,047格精确差异、42城索引及13城范围、18项摘要、拒绝重复迁移、从v50确定性重放。
- 原图指纹检查和42张原分辨率参考裁图生成成功，图像仅写入仓库外的私人交付目录。

## 构建、安装和发布证据

上述本地结果不等于Android安装通过。Actions单独执行编译、APK版本/固定签名/18项资源检查、API29正式地图安装探针，再推送主线；实际结果以对应run状态及 `installed-map51.txt` 为准。APK的准确运行提交记于制品 `BUILD_COMMIT`，逐文件传输摘要记于 `SOURCE_CHECKSUMS.json`，APK、源码ZIP和PATCH摘要记于 `SHA256SUMS`。截图必须来自安装后的真实MapView，不以裁图册替代。

源码只改地图资源和离线工具/测试，没有修改正式Java玩法或美术。87据点位置、591开发位、全部旧水域及保存格式v28不变。已有存档保留其地图；需新开局查看地理。ARM真机、帧率以及全国每条细支路未验收，不宣称100%还原。
