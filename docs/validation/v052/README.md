# v0.52 验证和交付边界

真实远端基线：`2ff086d25fffefc1ae4dd49805ce9201576b995d`。本地源包git基线SHA不同；使用逐文件旧/新SHA256验证的差量在真实远端提交上重放。发布只允许fast-forward，不覆盖并行新主线。准确运行源码见制品BUILD_COMMIT及APK内SOURCE_REVISION。

## 已执行的本地验证

`bash scripts/test-realm52.sh`：新增3,645项统计、关系等价、人员缓存失效、实际暴击来源及全国六旬金标断言通过。六旬heroes-250/28势力按固定种子12345正常推进，保存字节与修改前v51相同；不是通过更换小图或减少势力提速。最近回归还覆盖v48设施事件120项、v49部队粮耗忠诚338项、v50地图1446项、v51地图364项、港口AI及回放698项、持续目标93项、位移266项、设施产能73项、统一规则6290项；18项地图美术SHA未变。

独立本地同机六旬纯结算，修改前4.82/3.63/2.97/2.81/3.27/4.07秒，首轮缓存优化后2.57/2.03/1.03/0.97/1.25/1.23秒；进一步消除重复登用排序、热路径分配与AI移动整图序列化后，同机约0.5–1.2秒。最终脚本计时包含日志并在每旬解码后创建真实新世界；原始逐旬值以core-checks.txt为准，不作为Android总过场时间。

`bash scripts/test-ui-models.sh`通过51,837 UI/相机断言及451地形连接检查。

## Android验收门槛与产物

独立Actions编译app与测试APK并校验固定开发签名、版本52/0.52.0及18项资源。交付APK默认关闭debuggable（保留固定开发签名；开发者可用`-PdebuggablePlaytest=true`显式开启），避免将附加调试运行时作为正常游玩配置。最终性能验收不启用工作线程堆栈采样、不使用adb预先强制编译。API29/x86_64模拟器1080×1920、密度360、系统动画开启；Realm52Probe实际启动完整250剧本，连续六旬不手动跳过，逐次检查总过场<=10,000ms、最终金标字节、自动存档及恢复操作。不是仅运行Java基准后推断手机速度。只有实装日志出现REALM52 PASS才通过门槛。

同一探针还检查全国七类表格/计数/搜索、详情四页、真实地图点城与领地选择、AP/横屏、列表定位返回、暂停不阻塞计算、同任务重建恢复、真实战法680ms高光及跳过不重复伤害。独立Map51Probe再次检查安装后的实际地图。源码检查不替代截图；真实屏幕保存在screenshots。性能原始值在installed-realm52.txt及logcat-realm52.txt，测试完成汇总在v052-checks.txt。

本文件列出已执行本地结果和CI验收门槛；Android是否通过须以对应run实际状态、日志与截图为准。未进行ARM实体手机、低端设备、长期后期千军压力或所有历史剧本的完整人工验收。经济统计是明确的定期收支预测，非已发生全部开销流水。地图无本轮数据更新，不要求为此重新开局。

交付构建启用R8方法体优化，保留项目类、成员及枚举名称，避免破坏存档和实装反射验证；`-PdebuggablePlaytest=true`仍可使用不压缩的源码调试。暴击停驻从首个实际绘制帧计时，首帧即展示完整肖像，置于军事指令面板之上；实装验收使用PixelCopy读取真实窗口像素，不能只凭View引用存在判定高光已显示。
